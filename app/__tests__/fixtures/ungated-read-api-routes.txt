# Pages Router API routes with no authorization control. DECLARED DEBT.
# See __tests__/apiReadRouteAuthorization.test.ts. Its companion,
# apiRouteAuthorization.test.ts, covers MUTATING routes and their own
# reasoned UNGATED_BY_DESIGN allowlist.
#
# SEC-13 tranche 1 (2026-09-23): the guardAutomation routes are recognised as
# gated, and the public-by-design routes moved to EXEMPT with a reason each.
# SEC-13 tranche 2 (2026-09-23) gated 30 more with the sibling's key where no
# role that reaches the calling page loses it. Tranche 3 (2026-09-24) gates
# the rest on the keys of the screens each one serves, so the owner's Roles
# settings decide access (PLAN.md SEC-13). Left, each serving several screens:
#   axper/traffic          -- the home traffic card; gets its own key
#   products/index         -- product search in quote, POS, count, transfer,
#                             catalog and report flows
#   tills/index            -- the Till screen, the POS and till reconciliation
src/pages/api/axper/traffic/index.ts
src/pages/api/products/index.ts
src/pages/api/tills/index.ts
