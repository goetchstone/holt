{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Project-level Claude Code configuration. COMMITTED to repo so all sessions inherit the same enforcement. Hooks turn the skill checklists from optional reminders into pilot's-checklist gates -- see docs/FRAMEWORK.md for the three-layer model (CLAUDE.md / docs/domains / .claude/skills) and three enforcement mechanisms (skills = soft, hooks = hard gate, tripwire tests = CI backstop).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/pre-commit-check.sh",
            "timeout": 30,
            "statusMessage": "Pre-commit checklist..."
          },
          {
            "type": "command",
            "command": "./.claude/hooks/pre-pr-check.sh",
            "timeout": 30,
            "statusMessage": "Pre-PR checklist..."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./.claude/hooks/session-start-check.sh",
            "timeout": 30,
            "statusMessage": "Session orient..."
          }
        ]
      }
    ]
  }
}
