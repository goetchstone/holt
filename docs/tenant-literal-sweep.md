# Tenant-Literal Sweep — 67 Verified Findings

Generated by an 11-agent sweep (5 discovery lenses, adversarial refutation per lens,
then ranking). Every finding below survived a refute pass that opened the cited line.
**63 of 67 are new** — the plan knew about 4.

Blast radius: **18 money**, 21 data-correctness, 22 feature-dead, 6 cosmetic.

> Spot-verified by hand before publishing: the return-detection regex, the freight
> exclusion list, and the hardcoded CT tax rate. All three confirmed as described.

## How to read this — native vs import

Not all 67 cost the same. A finding on the **import path** is a one-time migration
concern: an operator maps it once when history is loaded, and holt is the system of
record thereafter. A finding on the **native path** is wrong on every transaction, in
every deployment, forever.

| | money | data | feature-dead | cosmetic | total |
|---|---:|---:|---:|---:|---:|
| **Native** — every deployment, forever | **13** | 14 | 17 | 5 | **49** |
| Import — one-time, per migration | 5 | 7 | 5 | 1 | 18 |

**Start with the native money tier.** These bite before anyone imports anything:

- `lib/commissionSales.ts:67`
- `lib/commissionTiers.ts:36`
- `lib/dateUtils.ts:8`
- `lib/goalsConfig.ts:31`
- `lib/homeAccessoryOrders.ts:297`
- `lib/journalEntry.ts:258`
- `lib/orderLineItemLinker.ts:200`
- `lib/payPeriod.ts:27`
- `lib/salesBySalesperson.ts:34`
- `lib/tillVariance.ts:38`
- `lib/tillVariance.ts:44`
- `pages/api/pricing/products.ts:279`
- `pages/api/proposals/[id]/convert-to-order.ts:90`

They cluster more tightly than the list suggests: **pay-period anchor, commission tiers,
bonus rate, split ratio, till thresholds** are one payroll-and-cash-policy group. That is a
coherent first piece of work — the settings a deployment must fix before it can pay anyone
correctly — rather than picking off literals one file at a time.

The import 18 are `apply-preset` material. Worth doing before an outside deployment migrates
history; not before one opens its doors.

> **A caveat on severity, learned the hard way in this session.** Every finding below was
> refuted by a second agent, and the three I then re-checked by hand were all real — but two
> had their blast radius overstated. `isReturnOrder` is the middle of three signals (an explicit
> status column runs first, a negative-total check after), so it is a false-*positive* risk
> rather than the revenue hole it reads as. Open the file before acting on a severity.

---

## money (18)

### `app/src/lib/adapters/ordorite/runners.ts:1500`

```
const vendorEntry = [...vendorCache.entries()].find(([, id]) => id === vendorId); if (!vendorEntry || !vendorEntry[0].includes("marjan")) continue;

```

**Breaks:** Any business running consignment through a vendor not literally named '...marjan...' never gets a ConsignmentPaymentBatch created on PO receipt, so the consignor is never paid out through holt and ConsignmentItems are never marked PAID. It fails silently -- the loop just `continue`s with no warning and no log entry.

**Fix:** Add Vendor.isConsignment boolean; both runners gate on `vendor.isConsignment` fetched with the vendor rather than substring-testing the cache key. Config (a preset or an admin toggle) selects which vendor is the consignor; the batch-creation behaviour stays in the runner per rule 62.

### `app/src/lib/adapters/ordorite/shared.ts:159`

```
const RETURN_STORE_SUFFIX = /^(SB|GT|CH|BB|WS|RS)[A-Z]*A\d/i;

```

**Breaks:** Another Ordorite tenant's store codes (e.g. `DALA0123`) match none of the six prefixes, so isReturnOrder returns false and every accounting return imports as a normal sale. Returns add to revenue instead of subtracting — daily sales, commissions and the sales journal all overstated by twice the return volume.

**Fix:** Keep the *shape* in code (an 'A' suffix on the store code is the Ordorite convention) and take the store-code alphabet from data: build the alternation from StoreLocation codes at call time, or add an `order-number-conventions` preset carrying `returnStoreCodes: [..]` and `returnPrefixes: [..]`. Config supplies the token list; the matching logic stays in the runner.

### `app/src/lib/adapters/ordorite/shared.ts:210`

```
if (/^RS\d/i.test(orderno)) return true;

```

**Breaks:** A deployment that uses `RS` for something that is not a return (e.g. a retail-stock series) has those orders classified RETURNED and dropped out of revenue; a deployment with a different return prefix has its real returns counted as sales. Wrong-direction revenue error either way.

**Fix:** Delete the line and fold `RS` into the data-driven return-prefix list from the fix above — one source for return-prefix tokens, none in source.

### `app/src/lib/adapters/ordorite/shared.ts:117`

```
const PAYMENT_MODE_MAP: Record<string, string> = { "1": "Finance", ..., "27": "Card Connect", "28": "Card Not Present", ... }

```

**Breaks:** Another Ordorite tenant numbering its modes differently gets code 27 labeled 'Card Connect'; the label lands verbatim in the free-text Payment.paymentType, and journalEntry.ts's paymentGlMap then fails to recognise it — payments that never reach a journal entry. runners.ts:2256 also gates phantom gift-card skipping on `resolvePaymentMode(...) === "Gift Card"`, so that safeguard silently stops working too.

**Fix:** Finish the preset that already exists: give `payment` an IMPORT_ENTITIES entry so the ordorite-payment-modes import-definition becomes live, have resolvePaymentMode read the active definition's valueMappings, and keep only 'Unknown'/passthrough behaviour in code. Ship the current table as the default preset, not as a literal.

### `app/src/lib/commissionSales.ts:67`

```
const multiplier = o.splitWithId ? 0.5 : 1;

```

**Breaks:** A business that splits 60/40, credits both partners at 100% (common where the split covers service rather than pay), or varies the split per order gets a commission base wrong by the difference on every split order. Because all six surfaces hold the same literal, the tier preview, the locked payout, the designer dashboard, monthly performance and the salesperson detail all agree with each other, so nothing flags the discrepancy. loadDesignerSaleRows feeds the Stage 1 rule engine's REVENUE/MARGIN/UNITS baskets (lines 144-148), so the error compounds through tier thresholds rather than staying linear.

**Fix:** Add SalesOrder.splitPercent (Decimal, nullable) so an uneven split is representable at all, plus a deployment default (AppSettings.defaultSplitCreditPercent, or a field on CommissionPlan if it should vary by plan) used when the order carries none. Then collapse all six sites onto one exported helper — creditMultiplier(order, defaultPercent) — so the number lives once. designerDashboard.ts:153 already has a creditMultiplier() function; widen that one and import it everywhere else.

### `app/src/lib/commissionTiers.ts:36`

```
{ minYtdSales: 0, maxYtdSalesExclusive: 750_000, rate: 0.03, label: "Up to $750k" },

```

**Breaks:** On a fresh deployment with no assigned plan, no isDefault plan and an empty CommissionTier table, every designer is silently commissioned on one employer's schedule, and the payout locks those rates into tierDefinitionSnapshot so the error persists after the tiers are corrected.

**Fix:** Make the code fallback refuse rather than guess: loadLegacyOrDefaultTiers() should return an empty/unconfigured result that payout preview and commit surface as a hard 'no commission plan configured' error, and move the 3-7% ladder into a shippable preset row (lib/config presets already seed DB state). Config SELECTS the ladder; source supplies none.

### `app/src/lib/dateUtils.ts:8`

```
const BUSINESS_TZ = "America/New_York";

```

**Breaks:** Every business-day boundary — daily sales, daily reconciliation, pay-period cutoffs, prior-day import windows — is computed Eastern. A Pacific deployment posts three hours of evening sales to the next business day: revenue wrong on both days, the reconciliation Over/Short plug fires, and sales move across commission periods.

**Fix:** BUSINESS_TZ becomes a resolved value from AppSettings.timezone (cached per request/process) rather than a module const, and the API-route duplicates call the same helper. AppSettings already selects the timezone; nothing else needs to be added.

### `app/src/lib/goalsConfig.ts:31`

```
export const DEFAULT_BONUS_RATE = 0.06;

```

**Breaks:** goals.ts:49 writes bonusRate ?? DEFAULT_BONUS_RATE onto every goal created without an explicit rate — the number is PERSISTED, not just displayed. A retailer paying 3% over goal has 6% silently stamped on new goal rows and shown as earned bonus on monthly performance until someone notices the payout is double.

**Fix:** An AppSettings.defaultBonusRate the org sets once; goalsConfig exports the resolver rather than the number. Better still, make bonusRate required at goal-creation time so no rate is ever silently supplied — a defaulted money rate is exactly the 'config supplies behaviour' shape rule 62 forbids.

### `app/src/lib/homeAccessoryOrders.ts:297`

```
export const SPLIT_PRESETS: Readonly<Record<number, readonly SplitPreset[]>> = { 2: [ { label: "62 / 38", percents: [62, 38] }, ... ] }

```

**Breaks:** A buyer accepting the prefill books 62% of a two-piece set price onto one piece and 38% onto the other. That cost lands on BuyerDraftItem.costPerUnit and flows into margin and buy-performance math. A retailer whose vendors split evenly or 70/30 gets systematically wrong per-piece cost unless every set is hand-edited.

**Fix:** Smallest honest fix: make Even the default prefill in shipped code, and move the named shapes to a per-vendor setting (a splitPresets JSON on the Vendor row) so a buyer who has real split data for their mill selects it. Config then selects which shape prefills; the cent-exact allocation math stays in code.

### `app/src/lib/journalEntry.ts:258` *(already on the backlog)*

```
const CASH_GL_CODES = ["1-1006"]; const DEPOSIT_GL_CODES = ["1-1200", "1-1203"]; const LIABILITY_DEBIT_CODES = ["2-2127"];

```

**Breaks:** Per the in-code note: a deployment numbering its cash account anything but '1-1006' falls to the generic else branch — cash receipts still post, but the deposit offset for an un-invoiced order and the gift-card liability debit both stop happening. The entry still balances because the Over/Short plug absorbs it, so the books are wrong with no unbalanced-day signal.

**Fix:** Re-derive all three from SystemGLMapping roles (CASH / CUSTOMER_DEPOSITS / GIFT_CARD_LIABILITY) rather than account numbers, exactly as the reconciliation read path now does. Because it changes which tender is treated as cash vs deposit vs liability, it wants its own change with before/after on real production tenders — as the note says.

### `app/src/lib/orderLineItemLinker.ts:200`

```
AND NOT (v_old.name ILIKE 'Marjan%' AND v_new.name ILIKE 'Marjan%')

```

**Breaks:** The clause protects intentional duplicate Product rows (one physical rug re-consigned repeatedly) from the UPC re-link. Another retailer's consignment vendor is not named Marjan, so the guard never fires and fixWrongLinks collapses their legitimate duplicate consignment products onto one product id, destroying the per-consignment sale/return history the payout run depends on.

**Fix:** Make it a property, not a name: add `Vendor.isConsignment Boolean @default(false)` (set by the same preset that will replace MARJAN_VENDOR_NAMES) and change the clause to `AND NOT (v_old."isConsignment" AND v_new."isConsignment")`. One flag then serves all four Marjan sites including buyPerformance.ts.

### `app/src/lib/payPeriod.ts:27`

```
export const PAY_PERIOD_ANCHOR_ISO = "2026-05-03"; // Sun — owner-confirmed period 5/03–5/16

```

**Breaks:** periodFromIndex (line 51) computes every boundary as anchor + 14n days. A semi-monthly, weekly, or week-offset bi-weekly retailer gets periods straddling their real ones, mis-slicing the pay-period sales report, commission payout runs, and the pay-period lock guard — commission computed over the wrong order set and payouts locked against the wrong window.

**Fix:** Two AppSettings fields — payPeriodAnchor (date) and payPeriodCadence (enum: WEEKLY | BIWEEKLY | SEMI_MONTHLY | MONTHLY). payPeriod.ts keeps all four cadence algorithms in shipped code and the setting selects which one runs plus its anchor; the existing custom start/end escape hatch on the statement page stays as-is.

### `app/src/lib/salesBySalesperson.ts:34`

```
const DELIVERY_FREIGHT_PRODUCT_NAMES = ["Delivery Charge", "Freight", "HD Freight", "Freight - Hunter Douglas", "Hunter Douglas Freight"] as const;

```

**Breaks:** A retailer whose freight products are named 'Shipping' or 'Delivery Fee' gets zero matches, so every freight dollar stays in the salesperson/designer credit base and commission is computed on inflated revenue.

**Fix:** Make exclusion a property of the product, not of its name: add `Product.excludeFromSalesCredit Boolean @default(false)` (or a reserved Category), set it from the admin UI / a preset that names existing products, and filter on the flag. Config selects which products are pass-through; the report logic stops knowing any names.

### `app/src/lib/tillVariance.ts:38`

```
export const TILL_VARIANCE_NOTE_THRESHOLD = 5; / TILL_VARIANCE_MANAGER_THRESHOLD = 20; / TILL_VARIANCE_ESCALATION_THRESHOLD = 100;

```

**Breaks:** A high-volume store routinely $30 over trips the manager tier constantly; a store $101 over has its register blocked against new opens (registers/[id]/tills/open.ts reads Register.blockedAt). A low-volume boutique wanting a $2 note threshold cannot tighten it. Only a redeploy moves them.

**Fix:** Three numeric fields on AppSettings (or a CashControlPolicy row): noteThreshold, managerThreshold, escalationThreshold, validated as ascending. tillVariance.ts keeps the four-tier classification and the block/escalate behaviour and takes the policy as an argument — config selects the cut points, never the enforcement. The user-facing strings at lines 95 and 131 already interpolate the constants, so they stay correct automatically.

### `app/src/lib/tillVariance.ts:44`

```
export const TILL_VARIANCE_ESCALATION_THRESHOLD = 100;

```

**Breaks:** Cash-control policy is one business's, hardcoded. A high-volume store where a $100 drawer variance is ordinary has registers blocking constantly and needs a MANAGER/ADMIN to clear each one before a till can open; a low-volume or non-USD store gets thresholds that mean nothing, since the numbers are currency-blind.

**Fix:** Add three integer fields to AppSettings (tillVarianceNote/Manager/EscalationMinor, stored in the currency's minor units so AppSettings.currency actually selects something) and have tillVariance.ts's classifier take them as arguments. The pure classification logic stays exactly where rule 14 wants it; config only supplies the three boundaries.

### `app/src/pages/api/consignment/import/manifest.ts:21` *(already on the backlog)*

```
const MARJAN_NAME_VARIANTS = ["Marjan International", "Marjan International Corp", "Marjan", "MARJANINT", "Marjan Int", "Marjan Intl"];

```

**Breaks:** findOrCreateMarjan resolves by these names, falls back to code MJ, and otherwise CREATES a vendor named 'Marjan International'. Any other retailer uploading a consignment manifest gets every item booked against a fabricated vendor in a fabricated 'Rugs' department, with payables computed against it.

**Fix:** Take the consignor as a required vendorId parameter on the manifest upload (chosen in the UI), and take departmentId/categoryId the same way — same fix as the pricing importers. Delete all three Marjan constants and the auto-create path; an importer must never invent a vendor or a department.

### `app/src/pages/api/pricing/products.ts:279`

```
if (group.name === "Pillow Upgrades" && !hasPillows) return [];

```

**Breaks:** A deployment that names the group 'Pillow Options' or 'Throw Pillows' gets no match, so pillow upgrade options are offered on every product including frames with no standard pillows. The configurator quotes an upcharge for pillows the frame does not have and the quote goes out wrong.

**Fix:** Add `VendorOptionGroup.requiresStandardPillows Boolean @default(false)` (or a general `appliesWhen` enum with a PILLOWS member) and test the flag instead of the name. The operator's naming stops being load-bearing; the admin UI selects the behaviour with a checkbox.

### `app/src/pages/api/proposals/[id]/convert-to-order.ts:90`

```
vatRate: isTaxExempt ? 0 : 0.0635,
              vatAmount: isTaxExempt ? 0 : Number(item.retailPrice) * item.quantity * 0.0635,

```

**Breaks:** Any deployment outside Connecticut that converts a B2B proposal to a sales order writes vatRate=0.0635 and a matching vatAmount onto every OrderLineItem. The rate is persisted, so it flows into journal-entry tax lines, daily reconciliation, and the QuickBooks export — not just the printed total. A district configured on the store or on AppSettings.defaultTaxDistrictId is ignored entirely on this path, and price-banded TaxRule rows never evaluate.

**Fix:** No new config needed — the config already exists and is unread. Import resolveTaxDistrict + rateForLineAmount from @/lib/tax/resolveTaxRate and resolve once per proposal (customer district -> store district -> AppSettings.defaultTaxDistrictId), then call rateForLineAmount per line exactly as create-from-cart.ts:123 and :252 do. Persist the resolved taxDistrictId on the order the same way. Delete both 0.0635 literals.

---

## data-correctness (21)

### `app/prisma/schema.prisma:1764`

```
@@unique([externalGipNo, externalPorNo])

```

**Breaks:** The model's only idempotency key is a pair of vendor-named nullable columns. Any other source adapter (AppSettings.sourceAdapterId is designed to be pluggable, lib/adapters/ has noneAdapter.ts + ordorite) has no re-import safety at all: NULLs compare distinct in a Postgres unique index, so a re-run appends duplicate receiving rows, which the buyersReport `received` CTE (lib/reports/buyersReport.ts:293-296) sums straight into allocated stock.

**Fix:** Replace the pair with the provenance shape this schema already uses twice — ServiceCase.externalSource/externalSourceId (schema.prisma:3378-3380) and InventorySnapshot.source/externalId (1324-1330): add `externalSource String?` + `externalRef String?` with @@unique([externalSource, externalRef]), and have the Ordorite runner write source="ordorite" and its composed key there. The adapter id (already config) SELECTS the namespace; no adapter-specific column stays on the core model.

### `app/src/components/sales/DenominationCountTable.tsx:15`

```
export const DENOMINATIONS: DenominationDef[] = [

```

**Breaks:** A deployment with currency set to CAD/GBP/EUR still counts US bills and coins on both the open-till and close-till flows. Denominations that do not exist locally are offered, real ones (£2, €200, loonie/toonie) cannot be entered, so calcTotal cannot reconcile the drawer — and that total is Till.actualCash, the input to the variance that drives the register-blocking logic in finding 10.

**Fix:** Ship a denomination table keyed by ISO currency code and select the row with AppSettings.currency; the component keeps taking its list as a prop exactly as it does today (the parent already owns state), and the `$` in fmt becomes the locale/currency formatter. Config selects which existing denomination set is used; it never supplies denominations as free-form data.

### `app/src/lib/adapters/ordorite/reportRouter.ts:74`

```
pattern: /Saybrook_Home_Inbound_Items/i,

```

**Breaks:** Another tenant's Ordorite export named `Acme_Furniture_Inbound_Items` never matches line 74, falls to line 79 and is fed to runPurchaseOrdersImport instead of runInboundItemsImport (wrong entity written). `Acme_Customers` matches nothing, resolveImportRoute returns null, and the customer master import is silently logged as skipped forever.

**Fix:** Move REPORT_ROUTES out of source into a `report-routing` preset kind: each row carries a filename `pattern` plus a `runnerKey` naming an entry in a compile-time RUNNERS registry (rule 62 — config selects the runner, never supplies the logic). Ship the current patterns as the default preset with the tenant tokens replaced by generic ones (`Inbound_Items`, `Customers`); config/local overrides the pattern list by name.

### `app/src/lib/apparelOrderVendors.ts:95`

```
export const APPAREL_VENDOR_FORMATS: readonly ApparelVendorFormat[] = [ { id: "rails", ..., catalogVendorName: "Rails", partNumberPrefix: "RAI", newItemsUseColorCode: true, ... } ]

```

**Breaks:** A different apparel retailer's brands have no row, so every upload falls to the unprefixed 'Other NuOrder' path: part numbers are composed without the vendor prefix and match nothing in their catalog, so every reorder line looks like a brand-new style and the buyer re-creates products that already exist.

**Fix:** Add `Vendor.partNumberPrefix String?` and resolve `catalogVendorName` from the selected Vendor record. The shipped registry then keeps only (formatId, label, accepts, parserKey) plus genuine format quirks; the vendor-specific facts come from the Vendor row the buyer picks. `newItemsUseColorCode` becomes `Vendor.colorKeyedByCode Boolean`; keep `normalizeStyleNumber` in code keyed by parser, since it is logic, not data.

### `app/src/lib/apparelOrderVendors.ts:168`

```
// Hardcoded (holt has no Vendor.partNumberPrefix column) -- value carried over verbatim from FC's DB-seeded prefix for Hunter Bell.\n    partNumberPrefix: "HBEL",

```

**Breaks:** Another retailer importing an apparel order gets either a wrong prefix (their Rails goods stamped 'RAI-' regardless of their own catalog scheme) or, for a vendor absent from the registry, the unprefixed fallback -- so the same physical product yields two different part numbers depending on which document format the order arrived in. extractSizeAndColor (line 316) then fails to round-trip because `partNumber.startsWith(head)` is false, and a reorder of an existing style creates duplicate draft items instead of matching.

**Fix:** Add the Vendor.partNumberPrefix column the header says was removed (or reuse the existing Vendor.code at schema.prisma:22, which already holds short vendor codes like 'WH'). The format registry drops partNumberPrefix entirely and buildPrefixedPartNumber takes the prefix from the resolved Vendor row; the fallback-to-unprefixed behaviour is unchanged.

### `app/src/lib/axperClient.ts:117`

```
HourMinuteFrom: opts.hourMinuteFrom ?? "09:00", HourMinuteTo: opts.hourMinuteTo ?? "18:00",

```

**Breaks:** A retailer open 10am-9pm loses every visitor counted after 6pm. Conversion rate (sales / traffic) is computed against a truncated denominator and reads systematically too high on the dashboard and in the traffic report.

**Fix:** Take the window from StoreLocation opening hours (or an AppSettings trafficWindow) and pass it in at each call site; keep the literals only as a last-resort fallback. Cheapest correct version: widen the shipped default to 00:00-23:59 and let the store record narrow it — the counter data is per-15-min anyway.

### `app/src/lib/barcode.ts:4`

```
return `SH-${vendorId}-${productId}-${rand}`;

```

**Breaks:** generateBarcode is called from CreateVariantView.tsx:219 and pages/api/purchasing/import-wholesale-order.ts:252, writing an unrelated company's initials into persisted Product barcodes that get printed on labels and scanned at the register — unfixable after the fact without reprinting the floor. The order-number prefix puts the same token on customer-facing documents.

**Fix:** One AppSettings.documentPrefix (short code, default derived from companyName, validated ^[A-Z0-9]{1,4}$). generateBarcode takes it as an argument and the two order-number builders read the same field, so all three identifier series carry the deploying company's own token. Keep the composition format in code — config supplies only the token.

### `app/src/lib/customerLeveling.ts:25`

```
const DEPT_GROUPS: Record<string, DeptGroupConfig> = { FURNITURE: { windowMonths: 12, departments: ["Furniture", "Outdoor Furniture", ...] }, ... }

```

**Breaks:** Another retailer's departments all land in HOME_ACC with a 12-month window: the APPAREL 6-month and CHRISTMAS seasonal windows never apply, the cross-shop bonus for 3+ groups never fires, and every customer is leveled against one distorted average. Levels drive the wealth/opportunity reports and marketing segments.

**Fix:** Move the grouping onto the Department record: add `Department.levelingGroup String?` + `Department.excludeFromLeveling Boolean`, and keep only the group definitions (name -> windowMonths, and the CHRISTMAS seasonal rule) in code. A `department-groups` preset assigns existing departments to groups by name; the code reads the assignment from the DB.

### `app/src/lib/homeAccessoryOrders.ts:85`

```
export const HOME_ACCESSORY_FORMATS: readonly HomeAccessoryFormat[] = [ { id: "kk-interiors", ..., catalogVendorName: "K & K Interiors" }, { id: "wendover", ..., catalogVendorName: "Wendover Art Group" }, ... ]

```

**Breaks:** Where a format nearly matches, `catalogVendorName` prefills a vendor name that does not exist in another tenant's Vendor table, producing draft POs attached to a newly-invented or wrong vendor; vendors outside these eight have no entry, so the Home Accessory Order Import tool is dead for them.

**Fix:** Drop `catalogVendorName` from the shipped registry and require the buyer to select the Vendor (or resolve it from the document, as MarketTime already does). If a default is wanted, hold it as a `sourceFormatId` field on the Vendor row — vendor data lives in the vendor table.

### `app/src/lib/importHelpers.ts:20`

```
if (s === "" || s === "@") return undefined;

```

**Breaks:** safeString is imported by lib/fmSafeMapper.ts, lib/genericImportRunner.ts, pages/api/purchasing/import-wholesale-order.ts and all five consignment import routes (pages/api/consignment/import/{sales,sales-lines,payment-lines,consignment-items,manifest}.ts) — none of which are Ordorite-fed. Any import whose cell legitimately holds a bare '@' has it coerced to undefined: a required field then fails validation with a message that does not explain why, and an optional field is dropped with no error at all.

**Fix:** Delete the `|| s === "@"` clause from safeString so it means only 'blank is blank'. If Ordorite genuinely needs it, express it where it belongs: either a small wrapper in lib/adapters/ordorite/shared.ts, or an optional `nullPlaceholders: string[]` on the import-definition preset schema (a list of literal sentinels — data, not an expression, so rule 62 holds) applied by the engine before coercion.

### `app/src/lib/pricing/beatrizBallOrderParser.ts:64`

```
"sold to", "ship to", "saybrook", "old saybrook",

```

**Breaks:** Another retailer's ship-to block prints their town and street, which match neither prefix, so those address lines are read as item rows or as description continuations — phantom line items or corrupted descriptions, and the printed-total reconciliation warning fires on every file.

**Fix:** Do not enumerate the buyer's address at all: drop the ship-to lines structurally (skip everything between the 'ship to'/'sold to' marker and the 'item code' header row), which is vendor-format logic and true for every buyer. If a literal list is still wanted, seed it from the tenant's own StoreLocation address at call time rather than from source.

### `app/src/lib/pricing/productEntryMapping.ts:57`

```
const VENDOR_DEPARTMENT: Record<string, string> = { ekornes: "Furniture", ..., "american leather": "Furniture" };

```

**Breaks:** A retailer carrying a different brand roster gets no VENDOR_DEPARTMENT hit, so department defaults to 'Furniture' and category to 'Living Room' for 100% of its catalog; CATEGORY_VALID_TYPES then coerces the inferred stock type to the first entry of a category the product does not belong to. Every product-entry suggestion is wrong.

**Fix:** Read the mappings from data the tenant already owns: vendor -> default Department via a `Vendor.defaultDepartmentId` column, POS supplier spelling via an existing `Vendor.code`/alias field, and collection -> category via a `vendor-taxonomy` preset of (vendor, collection) -> category rows. Keep only the keyword inference (TYPE_KEYWORDS) in code, since that is product language, not one tenant's roster.

### `app/src/lib/serviceCaseSheetImport.ts:666`

```
const PO_PREFIX_RE = /^PONO?\d/i;  // + PO_PATTERNS line 700, poNumberCandidates lines 719-725

```

**Breaks:** A business numbering POs 'PO-10234' or '2026-00412' gets [] from extractPoTokens and [] from poNumberCandidates on every row, so every imported service case is left with no purchase-order link and nothing is flagged. Compounding it, their real PO tokens are then not excluded from SALES_ORDER_PATTERNS at line 661, so 'PO10234' is captured as a sales order and the case is attached to the wrong record.

**Fix:** Delegate PO-token extraction to the active SourceAdapter (AppSettings.sourceAdapterId already exists at schema.prisma:4830) so the grammar lives beside the system that defines it, or carry a configured poNumberPrefix + pad width on AppSettings that the matcher reads. Either way the literals leave lib/ and the extraction logic stays in code.

### `app/src/lib/serviceCaseSheetImport.ts:661`

```
const SALES_ORDER_PATTERNS: RegExp[] = [/\b([A-Z]{2,5}\d{1,7}(?:\s*-\s*[A-Z])?)\b/gi];

```

**Breaks:** The suffix strip is a fallback that fires only after the exact orderno lookup misses, so its failure mode is silent: a deployment whose order numbers legitimately end in ' - A' as a distinct order (suite/variant marker, location letter, revision that is a different order rather than the same one rewritten) has the service case attached to the base order instead. matchSalesOrder returns customerId so the caller can use it as a customer fallback when phone/email matching fails (doc-comment at lines 293-297), so the wrong customer propagates onto the case too. Separately the base pattern [A-Z]{2,5}\d{1,7} assumes letters-then-digits, so deployments numbering orders '2024-0031' or '10425' extract no token at all.

**Fix:** Route it through the adapter registry that already exists. AppSettings.sourceAdapterId (schema.prisma:4822, default 'none') selects a SourceAdapter; give that interface an optional `orderNumberBase(orderno): string | null` capability and have matchSalesOrder call it, with 'none' returning null (no strip). Ordorite's implementation reuses REWRITE_SUFFIX_RE where it already lives. The token-extraction shape itself becomes an import-definition setting selecting from a small compile-time catalog of order-number shapes rather than a literal regex in source.

### `app/src/pages/api/inventory/export-variance.ts:11`

```
const APPAREL_DEPARTMENTS = ["Accessories", "Mens Apparel", "Womens Apparel"];

```

**Breaks:** A retailer whose apparel departments are named 'Ladies','Womenswear','Mens Clothing' -- or one with no apparel at all -- gets an empty apparel side on every variance report while 100% of counted units land in the non-apparel bucket, so scan-accuracy percentages and variance dollars are divided by the wrong denominator. Four duplicated copies means the wrong split can also drift between routes.

**Fix:** Add Department.isApparel boolean (or the DepartmentGroup FK from finding 2) and replace all four literal arrays with a single shared helper that filters on the flag. One config row per department selects the split; the four copies delete.

### `app/src/pages/api/inventory/variance-report.ts:10`

```
const APPAREL_DEPARTMENTS = ["Accessories", "Mens Apparel", "Womens Apparel"];

```

**Breaks:** Endpoints partition inventory with name: { in: APPAREL_DEPARTMENTS } / { notIn: ... }. A retailer with "Clothing"/"Shoes" classifies everything non-apparel: the apparel tab of the variance report, the on-hand-by-location split, the accurate-scan accuracy percentage, and the apparel/non-apparel CSV export all return empty or count on the wrong side.

**Fix:** A Department.countsAsApparel boolean (or a DepartmentClass enum on the Department row) set once in Admin. Replace the six copies with one filter helper in lib/inventory/ that builds the where clause from the flag — this fixes the rule-37 duplication and the rule-61 hardcode in the same edit, and the nullable-department notIn hazard disappears because the flag lives on the joined row.

### `app/src/pages/api/pricing/import/retail-grade-prices.ts:90`

```
where: { name: "Outdoor" }, create: { name: "Outdoor" },

```

**Breaks:** Importing a price list silently creates departments and categories in someone else's taxonomy. A retailer who files outdoor goods under "Patio" ends up with a phantom "Outdoor" department holding every imported product, splitting the catalog across two trees — which then desynchronizes department-grouped reports, the apparel/non-apparel inventory split, poGaps, and customer leveling, none of which know the two names mean the same thing.

**Fix:** Never create taxonomy from an importer. Make departmentId/categoryId required request parameters chosen by the operator in the import UI (defaulted from the Vendor's configured default department), and fail the import with a clear message when unset. That keeps applying an import idempotent and declarative per rule 63 — it writes products into taxonomy the retailer already declared, instead of inventing it.

### `app/src/pages/api/pricing/import/signature-elements.ts:221`

```
await tx.vendorStyle.updateMany({ where: { vendorId, styleNumber: { startsWith: "SE-" } }, data: { isDiscontinued: true } });

```

**Breaks:** A destructive updateMany driven by a string shape. Any style under that vendorId whose number happens to begin 'SE-' (a regional or unrelated series code) is flipped isDiscontinued=true on every Signature Elements import, silently pulling sellable styles from the catalog. Conversely a business running the equivalent program under a different code has hasSE permanently false, so the configurator's SE mode is unreachable.

**Fix:** Add VendorStyle.programId (FK to the existing VendorProgram relation on Vendor, schema.prisma:46) or a priceListId stamp written by the import run. The discontinue sweep scopes to `{ vendorId, programId }` and the UI splits on programId, not on the styleNumber text. The 'SE-' string survives only as generated display data, never as a query predicate.

### `app/src/pages/api/sales/import-hd-proposal.ts:65`

```
where: { name: "Hunter Douglas" }, update: {}, create: { name: "Hunter Douglas", code: "HD", pricingModel: "FLAT" },

```

**Breaks:** Any deployment using this importer gets a foreign Vendor, Department and Category injected into its own taxonomy. A retailer whose window coverings live under 'Soft Goods' now has an orphan 'Window Treatments' department that no report rolls up, plus labor/freight products whose names must exactly match salesBySalesperson.ts's list or freight is credited as sold revenue.

**Fix:** The endpoint takes vendorId / departmentId / categoryId / laborProductId / freightProductId as inputs resolved from an ImportDefinition preset row (or from the request, picked in the UI), and creates nothing it was not told to create. Order-number prefix comes from `Vendor.code`, not the literal `HD-`. If the referenced records are missing, fail with a clear config error rather than inventing them.

### `app/src/pages/api/sales/pipeline/index.ts:365`

```
if (lower.includes(s.displayName.toLowerCase())) { staffId = s.id; break; }

```

**Breaks:** Substring containment is not identity. With staff 'Ann' and 'Anna Reyes', every Anna quote matches 'ann' first-wins in whatever order the query returned, crediting quote count, total value and overdue count to the wrong designer. On the archive route the same collision is a privilege escalation -- Ann can archive Anna Reyes' quotes. And a POS writing 'Reyes, Anna' or an employee code matches nobody, so those quotes vanish from every pipeline.

**Fix:** On [id].ts:84 drop the name fallback outright -- authorization must use salesPersonId only. On index.ts:365 reuse the existing alias-aware exact-match helper from lib/salesBySalesperson.ts (displayName plus StaffMember.aliases, equality not containment), so the configurable field that already exists is the one consulted.

### `app/src/pages/api/service/dashboard.ts:33`

```
status: { isClosed: false, name: { contains: "Progress", mode: "insensitive" } }  // and "Waiting" at line 38

```

**Breaks:** A deployment naming its statuses 'Active','Working','On Hold','Pending Parts' -- or running in a non-English locale -- gets 0 on both the In Progress and Waiting tiles while Open Cases is non-zero: two headline KPIs read zero rather than erroring. Overlap is equally wrong: a status named 'Waiting on Progress Report' counts in both tiles, so the tiles can exceed the open-case total.

**Fix:** Add a semantic column to ServiceCaseStatus -- either two booleans (isInProgress, isWaiting) or a `kind` enum alongside the existing isClosed -- and count on the flag. The status name stays free text; a preset SELECTS which named status carries which kind.

---

## feature-dead (22)

### `app/prisma/schema.prisma:1072`

```
model WindfallEnrichment { ... windfallId String? @unique

```

**Breaks:** A retailer using a different enrichment provider (or none) carries a vendor-branded table hanging off Customer that nothing can populate, so Wealth Insights, the Opportunities tiles and lead scoring are dead features rather than provider-agnostic ones.

**Fix:** Rename to a provider-neutral `CustomerEnrichment` with a `provider String` + `externalId String` pair replacing `windfallId`, keep the signal booleans as the product's own vocabulary, and make the Windfall CSV parser one importer that maps into it. Then a second provider is a new importer, not a migration. (Rename only — the columns themselves are reasonable retail signals.)

### `app/prisma/schema.prisma:1131`

```
model MailchimpActivity {

```

**Breaks:** A retailer on Klaviyo/Constant Contact/Braze has no route into the lead pipeline — those two LeadSource values are the only campaign-driven sources — so campaign leads, the Marketing dashboard and campaign attribution are dead. Adding a second ESP requires a Postgres enum migration on a core model instead of a config row.

**Fix:** Collapse the two members to `EMAIL_CLICK` / `EMAIL_OPEN` and carry the provider on the campaign record the lead already references. One enum migration + backfill; the ingestor keeps its own vendor-specific parsing.

### `app/prisma/schema.prisma:1027`

```
windfallEnrichment  WindfallEnrichment?

```

**Breaks:** Five of the ten opportunity segments query the relation by name (lib/opportunityTiles.ts:144, 156, 166, 177, 210) and lib/reports/wealthInsights.ts:45 enumerates the provider's signal columns as SIGNAL_FIELDS. A retailer on a different enrichment provider cannot populate the relation without pretending to be Windfall, so 'Big wallets', 'Second home', 'Landlord special', 'Boat crowd' and 'Life event' all count zero and Wealth Insights renders blank. Swapping providers is a migration, not configuration.

**Fix:** Rename the model and relation to the domain concept — CustomerEnrichment / customerEnrichment — keeping the provider as data: a provider String column plus providerRecordId replacing windfallId, with @@unique([provider, providerRecordId]). The wealth/asset/life-event booleans are provider-neutral signals and stay as columns. One migration plus a mechanical rename across opportunityTiles.ts and wealthInsights.ts; the Windfall CSV importer becomes one adapter writing provider = "windfall".

### `app/prisma/schema.prisma:4123`

```
  MAILCHIMP_CLICK

```

**Breaks:** A retailer on any other ESP has no enum value for engagement-sourced leads and must file them as OTHER, collapsing them with walk-ins in every lead report, and lib/mailchimpLeadIngestor.ts is the only ingestion path. Because it is a Prisma enum, adding a provider is a migration — config cannot select it, which is the inversion rule 62 forbids.

**Fix:** Collapse the two brand values to one mechanism value (e.g. EMAIL_ENGAGEMENT) and carry the provider in the existing free-text Lead.sourceDetail, mirroring ServiceCase.externalSource (3378) which does exactly this job as a String. The provider name then lives in config/data, and adding an ESP is a registration, not a migration.

### `app/prisma/schema.prisma:3021`

```
model SEComponent {

```

**Breaks:** A retailer not carrying this vendor gets a dead table, a dead configurator (components/pricing/SEConfigurator.tsx) and a dead import route. One carrying a DIFFERENT build-your-own program cannot express it: its component types and codes are not in the shipped seed array and the importer is bound to one vendor's PDF parser.

**Fix:** The schema is already the right shape — delete SE_COMPONENT_SEEDS from the route and seed SEComponent rows from a vendor-scoped preset file (lib/config/presetFiles.ts) instead, with componentType a lookup rather than a comment-documented literal set. Register the PDF parser through the existing ImportDefinition.runnerKey escape hatch (schema.prisma:5480) so a second vendor's program is a preset + registered runner, not a code edit. The two vendor rule columns (notAvailableInLeather/notAvailableOnSleepers, 3032-3033) become rows in a generic component-restriction table.

### `app/prisma/schema.prisma:3923`

```
  rugNumber      String?

```

**Breaks:** A retailer consigning art, lighting or apparel has no generic item identifier — it must abuse rugNumber, and its manifest never parses because the uploader matches only that one workbook's headers and drops every row lacking rugNumber/quality. On top of that the 7x cost markup is one vendor's pricing arrangement applied to every consignment item on import.

**Fix:** Rename rugNumber/baleNumber to vendorItemRef/lotRef on ConsignmentItem with a config-supplied display label, and replace the literal header matching with an ImportDefinition field mapping (schema.prisma:5448, fieldMappings at 5484) so the workbook's column names are data. Move the 7x / 7-over-2 multipliers to a per-vendor consignment pricing setting; config SELECTS the multiplier, the pure calculation stays in code.

### `app/prisma/schema.prisma:3973`

```
  fmRecordId String?

```

**Breaks:** The consignment import route is written against one predecessor system's column names, so it is a dead feature for a retailer migrating consignment from anything else — there is no mapping surface, and the persisted column can never be populated. The generic answer already exists twice in this same schema (ServiceCase.externalSource/externalSourceId at 3378-3380, InventorySnapshot.source/externalId at 1324-1330), which is what makes this a gap rather than a design choice.

**Fix:** Replace fmRecordId with the existing pair `externalSource String?` + `externalSourceId String?` and drive the column names through ImportDefinition.fieldMappings (schema.prisma:5484) instead of literal row keys, so the predecessor system's header names become preset data. One migration plus swapping the hardcoded row.* reads for mapped lookups.

### `app/src/app/(dashboard)/app/admin/pricing/import/PricingImportView.tsx:109`

```
const VENDOR_CONFIGS: VendorConfig[] = [ { slug: "wesley-hall", nameMatch: "wesley hall", displayName: "Wesley Hall", ... } ]

```

**Breaks:** The entire pricing-import UI is driven off this array: a vendor absent from it has no upload path, and nameMatch silently fails to bind for a retailer whose Vendor rows are spelled differently. A dealer carrying eight other mills opens Admin > Pricing > Import and sees nine brands they do not buy from and none they do. The dated defaults also write stale price-list names into a new deployment's records.

**Fix:** Bind on Vendor.id, not a name substring. The per-vendor entry (which shipped parser, which import endpoint, label) moves onto the Vendor row or a VendorImportFormat table seeded by a preset; the view renders whatever the retailer's vendors declare. Drop the dated defaultPriceListName entirely and default the field to '<Vendor> <Type> <current month year>' computed at open time.

### `app/src/app/(dashboard)/app/tools/configurator/ConfiguratorView.tsx:92`

```
const wh = list.find((v) => v.name.toLowerCase().includes("wesley hall")); if (wh) { setSelectedVendorId(wh.id); } else if (list.length === 1) { ... }

```

**Breaks:** For any business not carrying Wesley Hall the find returns undefined. On the Options screen selectedVendorId stays null and the screen opens permanently empty with no vendor selected and no prompt; the other two screens behave differently for the same input, so the same deployment gets three inconsistent landing states.

**Fix:** Delete the literal from all three. Select the sole vendor when there is exactly one, otherwise leave unselected and render the existing empty-state prompt; if a default is genuinely wanted, read it from a Vendor.isDefaultForPricing flag or persist the user's last selection -- config selects the vendor, the screen supplies the behaviour.

### `app/src/components/pricing/SEConfigurator.tsx:138`

```
const isChair = ["C15","CMO","CHR","MOT","FSL","QSL"].includes(pieceType); const depthCode = isChair ? "CH" : depth; return `SE-${materialCode}${depthCode}-${pieceType}`;

```

**Breaks:** The configurator resolves the product by exact string equality on the constructed number (line ~146, `products.find((p) => p.productNumber === styleNumber)`). Any deployment, or any future price book, that codes chairs differently builds a style number matching nothing, so valid selections silently show 'no product'; and the arm surcharge is skipped for any piece the second list wrongly calls armless.

**Fix:** Add columns to the VendorOptionComponent rows the component already fetches: depthCodeOverride (nullable, 'CH') and isArmless / isOttoman booleans. Both the two inline arrays and seParser's duplicate ternary then read one DB source, so writer and reader cannot drift.

### `app/src/components/pricing/WoodPriceConfigurator.tsx:85`

```
const widthDim = useMemo(() => dimensions.find((d) => d.name === "Table Width"), [dimensions]);

```

**Breaks:** A deployment that names its dimensions 'Width (in)' or 'Top Length' gets widthDim/lengthDim/diameterDim all undefined, so the derived width/length option lists come back empty: the size pickers render with no options and no price can be computed, with no error explaining why.

**Fix:** Extend the dimension `type` vocabulary the file already relies on — TABLE_WIDTH / TABLE_LENGTH / TABLE_DIAMETER alongside WOOD_SPECIES — and match on `d.type`. The tenant keeps naming its dimensions whatever it likes; the type selects the role.

### `app/src/lib/adapters/ordorite/reportRouter.ts:49`

```
pattern: /SH_Stock_by_Item/i,

```

**Breaks:** A deployment whose Ordorite exports carry its own initials matches none of the three; resolveImportRoute returns null and the orchestrator logs 'skipped'. Stock-on-hand, PO-line reconciliation and the product catalog never populate, with no error surfaced.

**Fix:** Same fix as the Saybrook_Home routes — one preset-driven route table keyed pattern -> runnerKey. Do not fix by loosening the regex; the whole table is deployment data.

### `app/src/lib/axperClient.ts:43`

```
const AXPER_URL = "https://cloud.axper.com/api/TrafficReport/GetTrafficDataUsingDailyPeriod";

```

**Breaks:** An operator who fills in 'API Base URL' sees it silently ignored. A deployment on a different traffic counter, or on a regional/on-prem Axper instance, cannot point holt anywhere, so the traffic feature is dead and the Integration Test page reports pass/fail against someone else's cloud.

**Fix:** Resolve the base URL through the same credential path as apiKey (`resolveCredential('axper','apiBase')`), defaulting to the current literal when unset, and keep only the path segment in code. That is config selecting an endpoint, not supplying behaviour.

### `app/src/lib/inventory/snapshotImport.ts:46`

```
const stockIdStr = record.Stockid || record.stockid;

```

**Breaks:** A retailer whose stock export uses SKU/Qty/Bin gets stockIdStr undefined on every row, so resolveSnapshotImportRow returns { status: "skip" } for the whole file. The import completes reporting zero rows and no error, and the inventory variance workflow has nothing to compare against.

**Fix:** Resolve the three columns through the existing ImportDefinition field mapping before the row reaches this module: the caller hands resolveSnapshotImportRow a normalized { externalId, quantity, location } shape, and the header-to-field binding lives in the import definition the preset applies. Config selects which CSV header feeds which field; the resolve/skip/invalid classification stays in code untouched.

### `app/src/lib/opportunityTiles.ts:249`

```
id: "christmas-lapse", ... buildWhere: (now) => { const septThisYear = new Date(Date.UTC(now.getUTCFullYear(), 8, 1)); return { customerGroup: "CHRISTMAS", ... }; }

```

**Breaks:** Without a department literally named 'Christmas', getDeptGroup() falls through to DEFAULT_GROUP='HOME_ACC' and no Customer row ever holds customerGroup='CHRISTMAS'. The tile then queries a value nothing matches and renders a permanently-zero segment on the opportunity hub every October through December, with an avgEstPerCustomer of 400 attached to an empty set. For a business whose seasonal peak is spring or back-to-school the concept is right but the hardcoded group name and month window make it unreachable.

**Fix:** Fold into the customer-leveling group fix: the tile references a configured group key and reads its season window (seasonStartMonth / visibleFromMonth / visibleToMonth) off the CustomerLevelingGroup row rather than holding 'CHRISTMAS' and months 9-11 in source. shouldShow returns false when no such group is configured, so the tile disappears instead of rendering empty. The tile's copy and estimate move onto the group row too, or stay in code keyed by group kind — config selects which group the seasonal tile watches, it does not supply the query.

### `app/src/lib/pricing/productEntryMapping.ts:24`

```
const VENDOR_SUPPLIER_MAP: Record<string, string> = { ekornes: "Ekornes Inc", stressless: "Ekornes Inc", "wesley hall": "Wesley Hall", ... };

```

**Breaks:** getSupplierName (line 38) returns the input unchanged for an unknown vendor, so no normalization happens; the taxonomy suggester falls back to department "Furniture" / category "Living Room" for everything. A retailer buying from different mills gets every configured product suggested into one department/category pair — and if their catalog has no department literally named "Furniture", the suggestion is unusable and the copy-paste entry tool is dead weight.

**Fix:** Move all four maps into a pricing/product-entry preset kind in lib/config/presetSchema.ts, keyed by Vendor id rather than lowercase name substring, with department/category resolved to real Department/Category row ids at apply time. The keyword regex tables (PRODUCT_CATEGORY_KEYWORDS, TYPE_KEYWORDS) can stay in code as generic furniture vocabulary; the vendor and collection maps become data.

### `app/src/lib/reports/designerDashboard.ts:411`

```
partNo: { in: ["DC250", "DC-250", "DC 250"] },

```

**Breaks:** Another retailer's consultation SKU never matches, so hcOrders is empty. The whole House Calls panel — count, MTD/YTD/prior-year comparisons, attributed follow-up revenue over the -30/+90 window, conversion rate vs the $1,000 threshold — reads zero permanently with no error and no empty-state hint distinguishing misconfigured from genuinely zero.

**Fix:** A Product.isHouseCallCharge boolean (or one reserved system ProductRole row: HOUSE_CALL) that the retailer points at their own SKU in Admin. The report queries by the role, and renders an explicit 'no house-call product configured' state instead of a silent zero when nothing is mapped.

### `app/src/lib/reports/poGaps.ts:10`

```
const FURNITURE_DEPTS = new Set(["Furniture", "Outdoor Furniture", "Dining Room", "Bedroom", "Window Treatments"]);

```

**Breaks:** hasFurniture drives the missingESDFurniture and missingAckFurniture KPIs — the numbers a buyer chases. A retailer with "Case Goods"/"Upholstery" gets both KPIs pinned at 0 while genuinely un-acknowledged POs sit open: the exception report reports no exceptions.

**Fix:** Same Department-flag approach as the apparel split: a Department.requiresEsdAck boolean set in Admin. poGaps.ts keeps the 'flag lines whose department requires ESD/ack' rule and reads the flag; the empty-set case should surface as 'no departments configured' rather than a clean zero.

### `app/src/lib/serviceCaseSheetImport.ts:184`

```
const SHEETS_TO_IMPORT = ["C.S. In process", "C.S. Completed", "Repair"];

```

**Breaks:** Another retailer's workbook has differently named tabs, the loop matches zero sheets, and the import reports success with zero cases. Silent no-op with no signal that tab names are the problem.

**Fix:** Two parts, both cheap: (a) let the caller pass the sheet names (the upload UI can list the workbook's tabs and let the operator tick them), and (b) fail loudly — if no configured tab is present, return an error naming the tabs found. Keep the current three as the shipped default preset value, not as a source literal.

### `app/src/lib/serviceCaseSheetImport.ts:698`

```
const PO_PATTERNS: RegExp[] = [/\b(PONO\d{1,6})\b/gi, /\b(PON\d{1,6})\b/gi];

```

**Breaks:** A deployment whose source system numbers POs 'PO-12345' extracts zero PO tokens, so imported service cases never link to a purchase order and the vendor-claim workflow has nothing to attach to. In the other direction their real PO tokens now satisfy the generic sales-order pattern at line 661 and are looked up as sales orders, producing unmatched rows on every import.

**Fix:** Derive the PO prefix from data instead of a literal — read the distinct leading alpha prefix of existing PurchaseOrder.poNumber values, or carry `poNumberPrefix` in the same order-number-conventions preset as the return prefixes. Build both PO_PATTERNS and PO_PREFIX_RE from that one token so the two stay in step.

### `app/src/pages/api/consignment/return-items.ts:49`

```
where: { name: { contains: "Marjan", mode: "insensitive" } },

```

**Breaks:** For any business whose consignor is not named '...marjan...' all four endpoints -- return items, PO management, vendor-return import and POS backfill -- return empty or 404, so the consignment module is entirely dead. Because it is `contains` and not equality, a deployment with two vendors both containing the token gets a nondeterministic findFirst pick.

**Fix:** Same Vendor.isConsignment boolean as findings 11 and 12: all four lookups become `prisma.vendor.findFirst({ where: { isConsignment: true } })` (or take vendorId as a route parameter). One migration plus four one-line edits retires every name-substring consignment lookup in the codebase.

### `app/src/pages/api/google/create-project.ts:10`

```
const ROOT_FOLDER_ID = "1zlCFD_X19sw6PyFsTGMuhTE_buAv-mFK"; const TEMPLATE_PRESENTATION_ID = "14R696lLFtEjGAOoZt2XBo_UkBWVRo7aaHywHOqi4Mfs";

```

**Breaks:** drive.files.create at line 41 passes parents: [ROOT_FOLDER_ID]; another deployment's OAuth token has no access, so the call 404s/403s and the endpoint throws on first use. drive.files.copy of TEMPLATE_PRESENTATION_ID at line 79 fails identically. Create Project Folder is unusable out of the box and cannot be fixed without a redeploy.

**Fix:** Two nullable fields on the existing Google IntegrationCredential (or AppSettings): googleProjectRootFolderId and googlePresentationTemplateId, set in Admin. The endpoint returns a 400 with a 'Google project folders are not configured' message when either is absent instead of throwing a Drive error, so an unconfigured deployment gets a clear state rather than a stack trace.

---

## cosmetic (6)

### `app/prisma/schema.prisma:1815`

```
HD_PROPOSAL // imported from Hunter Douglas Direct Connect proposal

```

**Breaks:** A retailer with a different window-treatment supplier has a permanently unused enum member and cannot record its own proposal-import provenance without a Postgres enum migration plus backfill, so the vendor coupling hardens over time.

**Fix:** Rename the member to `VENDOR_PROPOSAL_IMPORT` in one migration and carry the actual vendor on the existing draft->Vendor relation. Fix it together with the endpoint below, not separately.

### `app/prisma/schema.prisma:2464` *(already on the backlog)*

```
  skipSameDayRewriteCleanup Boolean @default(false)

```

**Breaks:** A deployment that never runs the same-day-rewrite import carries a column on its highest-traffic table that can never mean anything, and every future reader of SalesOrder must learn an import-pipeline concept to understand the order model.

**Fix:** Already on the backlog: move the flag off SalesOrder into adapter-owned state keyed by order id, so the core model carries no import-runner concept.

### `app/prisma/schema.prisma:4697` *(already on the backlog)*

```
  axperStoreName String

```

**Breaks:** Because the brand-named column is NOT NULL and participates in the upsert key, a deployment using any other counter vendor must still populate a field named for one vendor to get idempotent imports. The generalized half (StoreLocation.trafficSourceNames, populated by a config preset) is already in place, so only the column name and the unique key remain.

**Fix:** Already on the backlog: rename to trafficSourceName and repoint the unique key, leaving the preset-populated StoreLocation.trafficSourceNames as the mapping surface.

### `app/src/lib/adapters/ordorite/reportRouter.ts:125`

```
/Marjan_Daily_Sales/i,

```

**Breaks:** Their redundant reports are not skipped and log as unknown on every run, adding noise; no data is lost or miscomputed by this literal alone.

**Fix:** None separately — SKIP_PATTERNS moves into the same preset-driven route table as REPORT_ROUTES (one `skip: true` route kind). Fixing findings 1 and 2 fixes this row for free.

### `app/src/pages/api/google/create-project.ts:58`

```
const subfolders = ["Windows", "Rugs", "Fabrics", "Furniture", "Photos", "Presentation"];

```

**Breaks:** Every customer project folder gets six subfolders named for merchandise categories the retailer may not carry. Renaming any requires a TypeScript edit, and renaming Presentation additionally breaks the template copy at line 79 with 'Could not find or create the Presentation subfolder.'

**Fix:** A projectFolderSubfolders string array in the same Google integration config, plus a separate presentationSubfolder field naming which one receives the template copy — so the load-bearing folder is selected by config rather than matched against a magic literal. Default the array in code to the current list.

### `app/src/pages/api/sales/orders/create-from-cart.ts:67`

```
const prefix = `SH-${yy}${mm}${dd}-`;

```

**Breaks:** Every order any deployment creates in holt's own POS is numbered SH-YYMMDD-NNN and every generated barcode starts SH-, carrying another company's initials onto physical merchandise tags and customer paperwork. The prefix is load-bearing, not cosmetic: the sequence generator queries `orderno: { startsWith: prefix }` (line 69) and parses the remainder with `orderno.replace(prefix, "")` (line 76), so changing it requires a coordinated edit across three files. Numbering itself keeps working, which is why this survives — the defect is branding and forkability, not broken math.

**Fix:** Add AppSettings.orderNumberPrefix (String, nullable) and AppSettings.barcodePrefix (String, nullable). Extract the duplicated generator into one helper — nextOrderNumber(tx, prefix) — used by both create-from-cart.ts and convert-to-order.ts so the startsWith query and the replace() parse can never drift, and pass the prefix into generateBarcode() instead of baking it in. Empty/null prefix means no prefix, not 'SH-'.

---

## Ranked worklist

# holt de-tenanting worklist

52 verified findings, grouped into 26 changes across 3 waves. **44 NEW, 8 already on the backlog.** Waves are ordered by blast radius first (money → data-correctness → feature-dead → cosmetic), then by migration cost. Everything in Wave 1 ships without touching `schema.prisma`; Wave 2 needs additive nullable columns (cheap migrations); Wave 3 needs renames/enum changes with backfill (slow, serialized behind main's ruleset — see MEMORY: CI is the verification path).

Baseline capture, do this once before Wave 1: dump the equivalence fixtures named below from a production snapshot into `/tmp` CSVs. Every "test" in this doc is a diff against those CSVs. Integration tests cannot run locally; each PR needs CI green.

---

## Wave 1 — money and correctness, no schema migration

### 1. Hardcoded CT VAT rate on proposal → order conversion — NEW, money
`app/src/pages/api/proposals/[id]/convert-to-order.ts:90-91`

```
vatRate: isTaxExempt ? 0 : 0.0635,
vatAmount: isTaxExempt ? 0 : Number(item.retailPrice) * item.quantity * 0.0635,

```

Persisted onto every `OrderLineItem`, so it flows into journal-entry tax lines, daily reconciliation and the QuickBooks export — not just the printed total. Price-banded `TaxRule` rows and `AppSettings.defaultTaxDistrictId` are ignored on this path only.

**Fix.** No new config. Import `resolveTaxDistrict` + `rateForLineAmount` from `@/lib/tax/resolveTaxRate`; resolve once per proposal (customer district → store district → `AppSettings.defaultTaxDistrictId`), call `rateForLineAmount` per line, persist the resolved `taxDistrictId` on the order. Copy the call shape from `app/src/pages/api/sales/orders/create-from-cart.ts:123` and `:252`. Delete both literals.

**Test (equivalence).** Convert every proposal converted in the last 12 months on a restored snapshot, old code vs new. Compare per-line `vatRate`, `vatAmount`, and order-level tax total. On the Saybrook district all must be byte-identical; any diff is a real pre-existing bug in the band logic and must be explained before merge. Then flip `defaultTaxDistrictId` to a non-CT district in a scratch DB and assert the rate changes.

**Why first.** Largest money error per event, one file, config already exists and is simply unread. Zero coupling to anything else on this list.

---

### 2. Ordorite return detection keyed to six store codes + an `RS` literal — NEW, money
`app/src/lib/adapters/ordorite/shared.ts:159` and `:210`

```
const RETURN_STORE_SUFFIX = /^(SB|GT|CH|BB|WS|RS)[A-Z]*A\d/i;
...
if (/^RS\d/i.test(orderno)) return true;   // "RS-prefixed orders are Returns Saybrook"

```

Another tenant's store codes match nothing → `isReturnOrder` false → every accounting return imports as a normal sale. Revenue, commissions and the sales journal overstate by **twice** the return volume (return not subtracted, and counted as a sale). Line 210 is wrong in both directions: an `RS` series that is not a return gets dropped out of revenue.

**Fix.** Keep the *shape* in code — the trailing `A` on the store code is the Ordorite convention, that's logic. Take the token alphabet from data: build the alternation from `StoreLocation` codes at call time, or add an `order-number-conventions` preset carrying `returnStoreCodes: []` and `returnPrefixes: []`. Fold `RS` into `returnPrefixes` and delete line 210 so there is exactly one source of return-prefix tokens. Ship Saybrook's current six as the default preset value.

**Test (equivalence).** Run `isReturnOrder` over every distinct `SalesOrder.orderno` in production (single query, no import needed) old vs new — the classified sets must be identical. Then re-run the last 90 days of Ordorite imports into a scratch DB and compare daily net sales by store and the sales-journal debit/credit totals. Add a unit case for `DALA0123` proving it classifies as a return once `returnStoreCodes` includes `DAL`.

**Pairs with #19** (`poNumberPrefix` lands in the same preset kind).

---

### 3. `BUSINESS_TZ` hardcoded Eastern — NEW, money
`app/src/lib/dateUtils.ts:8` — `const BUSINESS_TZ = "America/New_York";` plus the duplicated copies in API routes.

Every business-day boundary is Eastern: daily sales, daily reconciliation, pay-period cutoffs, prior-day import windows. A Pacific deployment posts three hours of evening sales to the next business day — revenue wrong on both days, the reconciliation Over/Short plug fires, and sales cross commission periods.

**Fix.** `BUSINESS_TZ` becomes a resolved value read from `AppSettings.timezone` (cached per process/request), not a module const. Find and repoint the API-route duplicates onto the same helper. `AppSettings.timezone` already exists — nothing to add.

**Test (equivalence).** With `AppSettings.timezone = "America/New_York"`, diff the daily-sales report, the daily reconciliation Over/Short line, and the import window boundaries for the last 400 days (must cover both DST transitions) — every row identical. Then set `America/Los_Angeles` on a scratch DB and assert the 8pm–11pm ET orders move to the prior business day.

---

### 4. `safeString` swallows a bare `@` for every importer — NEW, data-correctness
`app/src/lib/importHelpers.ts:20` — `if (s === "" || s === "@") return undefined;`

Imported by `lib/fmSafeMapper.ts`, `lib/genericImportRunner.ts`, `pages/api/purchasing/import-wholesale-order.ts` and all five `pages/api/consignment/import/*` routes — none of which are Ordorite-fed. A cell legitimately holding `@` becomes `undefined`: required fields fail validation with an unexplained message, optional fields drop silently.

**Fix.** Delete `|| s === "@"`. If Ordorite genuinely needs the sentinel, express it as a small wrapper in `lib/adapters/ordorite/shared.ts`, or an optional `nullPlaceholders: string[]` on the import-definition preset schema (a literal list is data, not an expression — rule 62 holds).

**Test.** Unit: `safeString("@") === "@"`. Then re-run one Ordorite import of each entity old vs new and diff row counts and null counts per column — expect zero diff unless Ordorite really does emit `@` as a null, in which case the wrapper is required and CI will show it.

---

### 5. Staff matched by substring containment — NEW, data-correctness + privilege escalation
`app/src/pages/api/sales/pipeline/index.ts:365` — `if (lower.includes(s.displayName.toLowerCase()))`
`app/src/pages/api/sales/pipeline/[id].ts:84` — same containment inside the archive authorization check

With staff `Ann` and `Anna Reyes`, every Anna quote is credited to whoever the query returned first, and on `[id].ts` **Ann can archive Anna Reyes' quotes**. A POS writing `Reyes, Anna` or an employee code matches nobody, so those quotes vanish from the pipeline entirely.

**Fix.** `[id].ts:84`: drop the name fallback outright — authorization uses `salesPersonId` only. `index.ts:365`: reuse the alias-aware exact-match helper already in `lib/salesBySalesperson.ts` (`displayName` plus `StaffMember.aliases`, equality not containment).

**Test.** Equivalence on attribution: dump quote count / total value / overdue count per designer from the pipeline endpoint before and after. Diffs are expected here and each one must be explained as a fixed mis-credit — write the list into the PR body. Authorization: a regression test asserting a designer whose `displayName` is a substring of another's gets 403 on the other's quote.

---

### 6. Destructive `SE-` discontinue sweep — NEW, data-correctness (data loss)
`app/src/pages/api/pricing/import/signature-elements.ts:220-223`

```
await tx.vendorStyle.updateMany({ where: { vendorId, styleNumber: { startsWith: "SE-" } }, data: { isDiscontinued: true } });

```

An `updateMany` driven by a string shape. Any style under that vendor whose number happens to start `SE-` (a regional or unrelated series) is flipped `isDiscontinued = true` on every import, silently pulling sellable styles from the catalog.

**Fix now (no migration).** Scope the sweep to rows the import run actually touched: collect the upserted style ids in the transaction and `updateMany({ where: { vendorId, id: { notIn: touchedIds } } })`, or stamp a `priceListId` on the run. The full program-scoping fix (`VendorStyle.programId`) is item #21 in Wave 2 — do not wait for it to stop the destructive write.

**Test.** On a scratch DB, insert a decoy `VendorStyle` under the SE vendor with `styleNumber: "SE-REGIONAL-1"` that the import file does not contain. Run the import; assert the decoy's `isDiscontinued` is unchanged, and that the count of styles discontinued by the run equals the count present-before-minus-present-in-file.

---

### 7. Ordorite report routing table is Saybrook filenames — NEW, data-correctness + feature-dead
`app/src/lib/adapters/ordorite/reportRouter.ts:74` — `pattern: /Saybrook_Home_Inbound_Items/i`
`app/src/lib/adapters/ordorite/reportRouter.ts:49` — `pattern: /SH_Stock_by_Item/i`
`app/src/lib/adapters/ordorite/reportRouter.ts:125` — `/Marjan_Daily_Sales/i` in `SKIP_PATTERNS` (cosmetic; free once the table moves)

`Acme_Furniture_Inbound_Items` falls past line 74 to line 79 and is fed to `runPurchaseOrdersImport` instead of `runInboundItemsImport` — **wrong entity written**. `Acme_Customers` matches nothing, `resolveImportRoute` returns null, and the customer master import logs as "skipped" forever. Same for the `SH_`-prefixed stock/PO-line/catalog routes: stock-on-hand and the product catalog never populate, no error surfaced.

**Fix.** Move `REPORT_ROUTES` and `SKIP_PATTERNS` out of source into one `report-routing` preset kind: each row = filename `pattern` + `runnerKey` naming an entry in a compile-time `RUNNERS` registry, plus a `skip: true` route kind for the skip list (rule 62 — config selects the runner, never supplies it). Ship the current patterns as the default preset with tenant tokens genericized (`Inbound_Items`, `Customers`). Do **not** fix by loosening the regexes; the whole table is deployment data.

**Test (equivalence).** Replay the last 90 days of received Ordorite filenames through `resolveImportRoute` old vs new; the (filename → runner | skip | null) mapping must be identical for all of them. Add a case asserting `Acme_Furniture_Inbound_Items` routes to the inbound-items runner under an overridden preset, and one asserting an unmatched filename returns a *loud* unknown rather than a silent skip.

**Blocks second-tenant onboarding.** Nothing else can be verified end-to-end on a non-Saybrook export until this lands.

---

### 8. Ordorite payment-mode code table — NEW, money
`app/src/lib/adapters/ordorite/shared.ts:117` — `PAYMENT_MODE_MAP: { "1": "Finance", ..., "27": "Card Connect", "28": "Card Not Present" }`

Labels land verbatim in the free-text `Payment.paymentType`; `journalEntry.ts`'s `paymentGlMap` then fails to recognise them, so **payments never reach a journal entry**. `lib/adapters/ordorite/runners.ts:2256` also gates phantom gift-card skipping on `resolvePaymentMode(...) === "Gift Card"`, so that safeguard silently stops working too.

**Fix.** Finish the preset that already half-exists: give `payment` an `IMPORT_ENTITIES` entry so the `ordorite-payment-modes` import-definition becomes live, have `resolvePaymentMode` read the active definition's `valueMappings`, and keep only `Unknown`/passthrough in code. Ship the current table as the default preset value.

**Test (equivalence).** Map every distinct raw mode code in production through old and new `resolvePaymentMode` — identical labels. Then diff the journal-entry lines by GL account for the last 90 days and the count of payments with no journal line (must not increase; ideally unchanged at whatever it is today). Assert the gift-card skip in `runners.ts:2256` still fires on the same order set.

---

### 9. Commission tier ladder guessed in source — NEW, money
`app/src/lib/commissionTiers.ts:36` — `{ minYtdSales: 0, maxYtdSalesExclusive: 750_000, rate: 0.03, label: "Up to $750k" }, ...`

On a fresh deployment with no assigned plan, no `isDefault` plan and an empty `CommissionTier` table, every designer is commissioned on one employer's 3–7% schedule — and the payout writes those rates into `tierDefinitionSnapshot`, so the error persists after the tiers are corrected.

**Fix.** Make the fallback refuse rather than guess: `loadLegacyOrDefaultTiers()` returns an unconfigured result that payout preview and commit surface as a hard "no commission plan configured" error. Move the 3–7% ladder into a shippable preset row (lib/config presets already seed DB state).

**Test (equivalence).** With Saybrook's tiers present in the DB, run the payout preview for the last 6 pay periods before and after — per-designer commission dollars identical. Then empty `CommissionTier` on a scratch DB and assert preview *errors* instead of returning numbers.

---

### 10. Consignment vendor identified by name substring — NEW (4 sites) + KNOWN (2 sites), money

| site | what |
|---|---|
| `app/src/lib/orderLineItemLinker.ts:200` | NEW — `AND NOT (v_old.name ILIKE 'Marjan%' AND v_new.name ILIKE 'Marjan%')` guard in `fixWrongLinks` |
| `app/src/lib/adapters/ordorite/runners.ts:1500` | NEW — `!vendorEntry[0].includes("marjan")` → `continue`, no payment batch on PO receipt |
| `app/src/pages/api/consignment/return-items.ts:49` | NEW — `name: { contains: "Marjan" }` (plus the sibling PO-management, vendor-return-import and POS-backfill routes) |
| `app/src/pages/api/consignment/import/manifest.ts:21` | KNOWN — `MARJAN_NAME_VARIANTS`, falls back to code `MJ`, else **creates** a vendor |
| `lib/consignment.ts` (`isMarjanRug`, `/^MAR-\d/`, `MARJAN_VENDOR_NAMES`) | KNOWN — backlog |
| `lib/reports/buyPerformance.ts` | KNOWN — backlog |

The `orderLineItemLinker` guard is the dangerous one: it protects intentional duplicate `Product` rows (one physical rug re-consigned repeatedly) from the UPC re-link. For a tenant whose consignor isn't named Marjan the guard never fires and `fixWrongLinks` **collapses legitimate duplicate consignment products onto one product id, destroying the per-consignment sale/return history the payout run depends on.** `runners.ts:1500` fails the other way: the consignor is never paid out and `ConsignmentItem`s are never marked PAID, silently, with no log line.

**Fix.** One additive column — `Vendor.isConsignment Boolean @default(false)` — set by the preset that replaces `MARJAN_VENDOR_NAMES`. Then:

- `orderLineItemLinker.ts:200` → `AND NOT (v_old."isConsignment" AND v_new."isConsignment")`
- `runners.ts:1500` → fetch the vendor with the row and gate on `vendor.isConsignment`; log a warning when no consignment vendor is configured instead of `continue`
- the four consignment routes → `findFirst({ where: { isConsignment: true } })`, or take `vendorId` as a route param
- `manifest.ts` → see #11; delete the auto-create path entirely

**Migration note.** One additive boolean + a backfill `UPDATE Vendor SET "isConsignment" = true WHERE name ILIKE 'Marjan%'`. Cheap, but it is a migration, so it serializes behind whatever else is in main's queue.

**Test (equivalence).** (a) `fixWrongLinks`: run it in a transaction on a production snapshot old vs new and diff the set of `(oldProductId → newProductId)` re-links — must be identical, and the Marjan pairs must appear in neither. (b) Payouts: re-run the last 6 months of PO receipts and compare `ConsignmentPaymentBatch` rows and amounts, and the count of `ConsignmentItem`s transitioned to PAID — identical. (c) A scratch DB with the consignment vendor renamed to `Acme Rugs` and `isConsignment = true` must produce the same batches.

---

### 11. Importers invent vendors, departments and categories — NEW, data-correctness
`app/src/pages/api/pricing/import/retail-grade-prices.ts:90` — `where: { name: "Outdoor" }, create: { name: "Outdoor" }`
`app/src/pages/api/sales/import-hd-proposal.ts:65` — `where: { name: "Hunter Douglas" }, create: { name: "Hunter Douglas", code: "HD", pricingModel: "FLAT" }` (+ a `Window Treatments` department and labor/freight products)
`app/src/pages/api/consignment/import/manifest.ts:21` — KNOWN, `findOrCreateMarjan` creates `Marjan International` in a fabricated `Rugs` department and computes payables against it

Importing a price list silently forks the tenant's taxonomy. A retailer filing outdoor goods under "Patio" ends up with a phantom "Outdoor" department holding every imported product, which then desynchronizes department-grouped reports, the apparel split (#17), `poGaps` (#17) and customer leveling (#17) — none of which know the two names mean the same thing. Violates rule 63: applying an import must be idempotent and declarative, not creative.

**Fix.** These three importers take `vendorId` / `departmentId` / `categoryId` (and for HD, `laborProductId` / `freightProductId`) as required inputs, resolved from an `ImportDefinition` preset row or chosen by the operator in the import UI, defaulted from the vendor's configured default department. They create **nothing** they were not told to create. Missing referenced records → a clear config error, not an invention. HD's order-number prefix comes from `Vendor.code`, not the literal `HD-`.

**Test.** Idempotency: run each importer twice against the same file on a snapshot and assert zero new `Vendor`/`Department`/`Category`/`Product` rows on the second run *and* zero on the first when the ids are supplied. Equivalence: with Saybrook's real ids passed in, diff the resulting `Product` rows (department, category, vendor, prices) against a pre-change run — identical. Assert a 400 with a named config error when an id is absent.

---

### 12. Journal-entry GL codes — KNOWN, money
`app/src/lib/journalEntry.ts:258` — `CASH_GL_CODES = ["1-1006"]; DEPOSIT_GL_CODES = ["1-1200","1-1203"]; LIABILITY_DEBIT_CODES = ["2-2127"];`

Per the in-code note: another chart of accounts falls to the generic else branch — cash receipts still post, but the deposit offset for an un-invoiced order and the gift-card liability debit both stop happening. The entry still balances because the Over/Short plug absorbs it, **so the books are wrong with no unbalanced-day signal.**

**Fix.** Re-derive all three from `SystemGLMapping` roles (CASH / CUSTOMER_DEPOSITS / GIFT_CARD_LIABILITY), exactly as the reconciliation read path already does.

**Test (equivalence).** This changes which tender is treated as cash vs deposit vs liability, so it needs its own PR with before/after on **real production tenders**: regenerate journal entries for 90 days and diff every line by (date, GL account, debit, credit). Every line identical, and the Over/Short plug must be identical to the cent. Any diff means the role mapping disagrees with the account numbers and must be resolved with the owner before merge.

**Ordering.** Money-critical but it is on the backlog and needs owner sign-off on the mapping — do not let it block Wave 1 items 1–11. Ship it as the last Wave 1 PR.

---

## Wave 2 — additive nullable columns / booleans (cheap migrations, one per PR)

### 13. Product-level report roles: freight exclusion + house calls — NEW, money + feature-dead
`app/src/lib/salesBySalesperson.ts:34` — `DELIVERY_FREIGHT_PRODUCT_NAMES = ["Delivery Charge","Freight","HD Freight","Freight - Hunter Douglas","Hunter Douglas Freight"]`
`app/src/lib/reports/designerDashboard.ts:411` — `partNo: { in: ["DC250","DC-250","DC 250"] }`

Freight: a retailer whose products are named "Shipping"/"Delivery Fee" gets zero matches, so freight dollars stay in the commission base and **commission is computed on inflated revenue**. House calls: another consultation SKU never matches, so the entire House Calls panel — count, MTD/YTD/prior-year, attributed follow-up revenue over the −30/+90 window, conversion vs the $1,000 threshold — reads zero permanently, with no empty state distinguishing misconfigured from genuinely zero.

**Fix.** One migration, two flags on `Product`: `excludeFromSalesCredit Boolean @default(false)` and `isHouseCallCharge Boolean @default(false)` (or one reserved `ProductRole` table with SALES_CREDIT_EXCLUDED / HOUSE_CALL rows). Set from Admin; a preset can name Saybrook's existing products. Reports query the flag. `designerDashboard` renders "no house-call product configured" instead of a silent zero.

**Test (equivalence).** Backfill the flags to exactly the products the literals match today, then diff: per-designer credited revenue per month for 24 months (must be identical to the cent), and every House Calls tile value for the same window. Then unset the house-call flag and assert the panel renders the configured-nothing state, not zeros.

---

### 14. Split-order credit fixed at 50%, and a persisted 6% bonus rate — NEW, money
`app/src/lib/commissionSales.ts:67` — `const multiplier = o.splitWithId ? 0.5 : 1;` (same literal at six sites)
`app/src/lib/goalsConfig.ts:31` — `export const DEFAULT_BONUS_RATE = 0.06;` (written to the row by `goals.ts:49`)

A business splitting 60/40, or crediting both partners at 100%, gets a wrong commission base on every split order — and because all six surfaces hold the same literal (tier preview, locked payout, designer dashboard, monthly performance, salesperson detail), **they all agree with each other so nothing flags it.** `loadDesignerSaleRows` feeds the Stage 1 rule engine's REVENUE/MARGIN/UNITS baskets (`commissionSales.ts:144-148`), so the error compounds through tier thresholds rather than staying linear. The bonus rate is worse than a display default: `bonusRate ?? DEFAULT_BONUS_RATE` is **persisted** onto new goal rows and shown as earned bonus until someone notices the payout is double.

**Fix.** `SalesOrder.splitPercent Decimal?` so an uneven split is representable at all, plus `AppSettings.defaultSplitCreditPercent` (or a `CommissionPlan` field if it varies by plan) used when the order carries none. Collapse all six sites onto one exported `creditMultiplier(order, defaultPercent)` — widen the existing `creditMultiplier()` at `designerDashboard.ts:153` and import it everywhere. For the bonus: `AppSettings.defaultBonusRate`, and `goalsConfig` exports a resolver, not a number. Better: make `bonusRate` **required** at goal creation so no money rate is ever silently supplied (rule 62).

**Test (equivalence).** With `defaultSplitCreditPercent = 50` and `defaultBonusRate = 0.06`, diff all six surfaces for 24 months: tier preview commission per designer, committed payout totals, designer dashboard credited revenue, monthly performance bonus earned, salesperson detail. Every number identical. Then set 60 on a scratch DB and assert exactly the split orders move, by the expected delta. Separately assert new goals created without an explicit rate are rejected (or carry the AppSettings value, never 0.06).

---

### 15. Pay-period anchor and cadence hardcoded — NEW, money
`app/src/lib/payPeriod.ts:27` — `export const PAY_PERIOD_ANCHOR_ISO = "2026-05-03"; // owner-confirmed period 5/03–5/16`, with `periodFromIndex` (line 51) computing anchor + 14n.

A semi-monthly, weekly or week-offset bi-weekly retailer gets periods straddling their real ones — the pay-period sales report mis-sliced, commission computed over the wrong order set, payouts locked against the wrong window.

**Fix.** Two `AppSettings` fields: `payPeriodAnchor` (date) and `payPeriodCadence` (enum WEEKLY | BIWEEKLY | SEMI_MONTHLY | MONTHLY). All four cadence algorithms stay in shipped code; the setting selects which one runs and its anchor. The statement page's custom start/end escape hatch stays as-is.

**Test (equivalence).** With anchor `2026-05-03` + BIWEEKLY, enumerate every period boundary from the earliest order to +1 year and diff against the current implementation — identical dates. Then diff pay-period sales totals and payout order sets for the last 6 periods. Add unit coverage for SEMI_MONTHLY boundaries across a February and a 31-day month.

---

### 16. Cash-control thresholds and US-only denominations — NEW, money + data-correctness
`app/src/lib/tillVariance.ts:38` and `:44` — `NOTE = 5; MANAGER = 20; ESCALATION = 100;`
`app/src/components/sales/DenominationCountTable.tsx:15` — `export const DENOMINATIONS: DenominationDef[] = [` (US bills and coins)

The thresholds are currency-blind and enforce one business's policy: a store $101 over has its register blocked (`registers/[id]/tills/open.ts` reads `Register.blockedAt`) and needs a MANAGER/ADMIN to clear each one; a boutique wanting a $2 note threshold cannot tighten it. The denomination table is the input to `Till.actualCash`, which is the input to that variance — a CAD/GBP/EUR deployment cannot even enter £2 or a toonie, so the drawer never reconciles and the blocking logic fires on a number that means nothing.

**Fix.** Three integer fields on `AppSettings` stored in the currency's **minor units** (`tillVarianceNoteMinor` / `ManagerMinor` / `EscalationMinor`, validated ascending) so `AppSettings.currency` actually selects something. `tillVariance.ts` keeps the four-tier classification and the block/escalate behaviour and takes the policy as an argument. Ship a denomination table keyed by ISO currency code and select the row by `AppSettings.currency`; the component already takes its list as a prop and the parent already owns state, so only the source of the list changes. The `$` in `fmt` becomes a locale/currency formatter. The user-facing strings at `tillVariance.ts:95` and `:131` already interpolate the constants and stay correct automatically.

**Test (equivalence).** Replay every historical `Till` close through the classifier old vs new with the policy set to 5/20/100 — identical tier for every till, and identical set of tills that would have blocked a register. Snapshot the open-till and close-till forms under USD and assert the rendered denomination rows are unchanged; then set GBP and assert the £ set renders and `calcTotal` reconciles.

---

### 17. Department classification: apparel split, ESD/ack scope, leveling groups, seasonal tile — NEW, data-correctness

| site | literal |
|---|---|
| `app/src/pages/api/inventory/variance-report.ts:10` | `APPAREL_DEPARTMENTS = ["Accessories","Mens Apparel","Womens Apparel"]` |
| `app/src/pages/api/inventory/export-variance.ts:11` | same array (4 copies total across inventory routes) |
| `app/src/lib/reports/poGaps.ts:10` | `FURNITURE_DEPTS = new Set(["Furniture","Outdoor Furniture","Dining Room","Bedroom","Window Treatments"])` |
| `app/src/lib/customerLeveling.ts:25` | `DEPT_GROUPS` with FURNITURE / APPAREL / CHRISTMAS / HOME_ACC membership + window months |
| `app/src/lib/opportunityTiles.ts:249` | `christmas-lapse` tile querying `customerGroup: "CHRISTMAS"`, months 9–11, `avgEstPerCustomer: 400` |

Apparel: a retailer with "Clothing"/"Shoes" classifies everything non-apparel — the apparel tab, the on-hand-by-location split, the scan-accuracy percentage and the CSV export all count on the wrong side, and the four copies can drift. `poGaps`: `missingESDFurniture`/`missingAckFurniture` — the KPIs a buyer chases — pin at 0 for a "Case Goods"/"Upholstery" tenant **while genuinely un-acknowledged POs sit open**. Leveling: everything lands in HOME_ACC at 12 months, the APPAREL 6-month and CHRISTMAS seasonal windows never apply, the cross-shop bonus for 3+ groups never fires, and every customer is leveled against one distorted average — which drives the wealth/opportunity reports and marketing segments. The Christmas tile then queries a `customerGroup` value nothing can ever hold and renders a permanently-zero segment every Oct–Dec.

**Fix (one migration, three independent call-site commits).** Add to `Department`: `countsAsApparel Boolean`, `requiresEsdAck Boolean`, `levelingGroup String?`, `excludeFromLeveling Boolean`. Then:

- **17a** replace the four apparel arrays with one shared filter helper in `lib/inventory/` that builds the where clause from the flag — this kills the rule-37 duplication and the rule-61 hardcode in one edit, and the nullable-department `notIn` hazard disappears because the flag lives on the joined row.
- **17b** `poGaps.ts` reads `requiresEsdAck`; an empty configured set surfaces as "no departments configured", not a clean zero.
- **17c** `customerLeveling.ts` keeps only the group definitions (group key → windowMonths, and the seasonal rule) in code and reads membership from the DB; the seasonal tile references a configured group key and reads `seasonStartMonth` / `visibleFromMonth` / `visibleToMonth` off the group row, with `shouldShow` returning false when no seasonal group is configured so the tile disappears instead of rendering empty.

**Test (equivalence).** Backfill the flags to exactly today's literal membership, then diff, before vs after: (a) variance report apparel/non-apparel unit and dollar totals, the accuracy percentage, and both CSV exports row-for-row; (b) `missingESDFurniture` / `missingAckFurniture` counts and the exception row ids; (c) `Customer.level` for **every** customer, plus all ten opportunity-tile counts. Identical throughout. Then rename a department in a scratch DB and assert the flag, not the name, still drives the split.

---

### 18. Service dashboard tiles keyed to status *names* — NEW, data-correctness
`app/src/pages/api/service/dashboard.ts:33` and `:38` — `name: { contains: "Progress" }` / `contains: "Waiting"`

A tenant naming statuses "Active"/"Working"/"On Hold"/"Pending Parts" — or running non-English — gets 0 on both headline tiles while Open Cases is non-zero. Overlap is equally wrong: "Waiting on Progress Report" counts in both, so the tiles can exceed the open-case total.

**Fix.** Add a semantic column to `ServiceCaseStatus` alongside the existing `isClosed` — either `isInProgress` + `isWaiting` booleans or a `kind` enum. Count on the flag; the status name stays free text and a preset SELECTS which named status carries which kind.

**Test (equivalence).** Backfill the flags from today's name matches, diff both tile counts daily for 12 months — identical. Add an assertion that in-progress + waiting + other ≤ open total (today it can exceed).

---

### 19. Service-case sheet import: tab names and PO/order-number grammar — NEW, feature-dead + data-correctness
`app/src/lib/serviceCaseSheetImport.ts:184` — `SHEETS_TO_IMPORT = ["C.S. In process","C.S. Completed","Repair"]`
`app/src/lib/serviceCaseSheetImport.ts:698` — `PO_PATTERNS = [/\b(PONO\d{1,6})\b/gi, /\b(PON\d{1,6})\b/gi]`
`app/src/lib/serviceCaseSheetImport.ts:666` — `PO_PREFIX_RE = /^PONO?\d/i` (+ `poNumberCandidates`, lines 719-725)
`app/src/lib/serviceCaseSheetImport.ts:661` — `SALES_ORDER_PATTERNS = [/\b([A-Z]{2,5}\d{1,7}(?:\s*-\s*[A-Z])?)\b/gi]`

Tabs: another workbook's names match zero sheets and the import **reports success with zero cases**. PO grammar: a tenant numbering POs `PO-12345` extracts no tokens, so cases never link to a PO and the vendor-claim workflow has nothing to attach to — and worse, their real PO tokens then satisfy the sales-order pattern at 661 and are looked up as sales orders, attaching cases to the **wrong record**. The `- A` suffix strip is a silent fallback: a tenant whose order numbers legitimately end in `" - A"` as a distinct order gets the case attached to the base order, and since `matchSalesOrder` returns `customerId` as a fallback when phone/email matching fails (doc-comment lines 293-297), the **wrong customer propagates onto the case**. The `[A-Z]{2,5}\d{1,7}` shape also assumes letters-then-digits, so `2024-0031` or `10425` extract nothing.

**Fix.** (a) Sheet names: caller-supplied (the upload UI lists the workbook's tabs and the operator ticks them), current three ship as the default preset value, and **fail loudly** — if no configured tab is present, return an error naming the tabs found. (b) PO grammar: derive the prefix from data — read the distinct leading alpha prefix of existing `PurchaseOrder.poNumber`, or carry `poNumberPrefix` + pad width in the same `order-number-conventions` preset as #2 — and build `PO_PATTERNS` and `PO_PREFIX_RE` from that one token so they cannot drift. (c) The suffix strip goes through the adapter registry that already exists: `AppSettings.sourceAdapterId` (`schema.prisma:4822`, default `none`) selects a `SourceAdapter`; give that interface an optional `orderNumberBase(orderno)` and have `matchSalesOrder` call it, with `none` returning null (no strip) and Ordorite reusing `REWRITE_SUFFIX_RE` where it already lives. The token shape becomes an import-definition setting selecting from a small compile-time catalog of order-number shapes.

**Test (equivalence).** Re-import the same production workbook old vs new and diff: case count per tab, the `(case → purchaseOrderId)` set, the `(case → salesOrderId)` set, and the `(case → customerId)` set. All identical. Then a scratch run with `PO-12345`-style numbers asserting non-empty PO links and no PO token captured as a sales order; and an assertion that a workbook with no matching tabs returns an error naming the tabs found.

---

### 20. `SH-` on barcodes and order numbers — NEW, cosmetic-but-persisted
`app/src/lib/barcode.ts:4` — `` return `SH-${vendorId}-${productId}-${rand}`; `` (called from `CreateVariantView.tsx:219` and `pages/api/purchasing/import-wholesale-order.ts:252`)
`app/src/pages/api/sales/orders/create-from-cart.ts:67` — `` const prefix = `SH-${yy}${mm}${dd}-`; ``

Another company's initials get printed on physical merchandise tags and onto customer paperwork — unfixable after the fact without reprinting the floor. The prefix is **load-bearing**, not decorative: the sequence generator queries `orderno: { startsWith: prefix }` (line 69) and parses the remainder with `orderno.replace(prefix, "")` (line 76), so changing it needs a coordinated edit across three files.

**Fix.** `AppSettings.orderNumberPrefix String?` and `AppSettings.barcodePrefix String?` (short code, default derived from `companyName`, validated `^[A-Z0-9]{1,4}$`). Extract one `nextOrderNumber(tx, prefix)` helper used by both `create-from-cart.ts` and `convert-to-order.ts` so the `startsWith` query and the `replace()` parse can never drift, and pass the prefix into `generateBarcode()`. Empty/null means **no prefix**, not `SH-`.

**Test.** With prefix `SH`, create 200 orders concurrently on a scratch DB and assert the sequence is gapless and matches what the old generator produces for the same day. Set prefix to `ACME` and assert numbering restarts correctly and the `startsWith` query finds only the new series. Existing barcodes are immutable — the PR must state that historical `SH-` barcodes are left alone and only new ones carry the configured token.

---

### 21. Configurator behaviour keyed to option/dimension *names* — NEW, money + feature-dead
`app/src/pages/api/pricing/products.ts:279` — `if (group.name === "Pillow Upgrades" && !hasPillows) return [];`
`app/src/components/pricing/WoodPriceConfigurator.tsx:85` — `dimensions.find((d) => d.name === "Table Width")` (+ length, diameter)
`app/src/components/pricing/SEConfigurator.tsx:138` — `isChair = ["C15","CMO","CHR","MOT","FSL","QSL"].includes(pieceType)`, `` `SE-${materialCode}${depthCode}-${pieceType}` ``

Pillows: a tenant naming the group "Pillow Options"/"Throw Pillows" gets no match, so pillow upgrades are offered on frames with no standard pillows and **the quote goes out with an upcharge for pillows that don't exist**. Wood: names like "Width (in)" leave `widthDim`/`lengthDim`/`diameterDim` undefined, the size pickers render empty and no price can be computed, with no error explaining why. SE: the built style number is matched by exact string equality (`products.find((p) => p.productNumber === styleNumber)`, ~line 146), so any other chair coding — or any future price book — matches nothing and valid selections silently show "no product"; the arm surcharge is skipped for anything the second list wrongly calls armless.

**Fix.** Make each a property, not a name. `VendorOptionGroup.requiresStandardPillows Boolean @default(false)` (or a general `appliesWhen` enum with a PILLOWS member) — the admin UI selects the behaviour with a checkbox. Extend the dimension `type` vocabulary the wood file already relies on: TABLE_WIDTH / TABLE_LENGTH / TABLE_DIAMETER alongside WOOD_SPECIES, and match on `d.type`. Add `depthCodeOverride String?` ('CH'), `isArmless Boolean` and `isOttoman Boolean` to the `VendorOptionComponent` rows `SEConfigurator` already fetches, so the two inline arrays **and `seParser`'s duplicate ternary** read one DB source and writer/reader cannot drift.

**Test (equivalence).** Enumerate every reachable configuration on the current data and diff the priced result: for pillows, the set of (product, offered option groups) and the quoted total; for wood, the width/length/diameter option lists per product; for SE, the constructed style number and resolved product for every (material, depth, pieceType) combination, plus whether the arm surcharge applied. Identical throughout. Then rename each name-matched record and assert behaviour is unchanged.

---

### 22. Vendor-bound pricing UI and product-entry mapping — NEW, feature-dead + data-correctness
`app/src/app/(dashboard)/app/admin/pricing/import/PricingImportView.tsx:109` — `VENDOR_CONFIGS` with `nameMatch: "wesley hall"` etc., plus dated `defaultPriceListName`s
`app/src/app/(dashboard)/app/tools/configurator/ConfiguratorView.tsx:92` — `list.find((v) => v.name.toLowerCase().includes("wesley hall"))`
`app/src/lib/pricing/productEntryMapping.ts:24` — `VENDOR_SUPPLIER_MAP`
`app/src/lib/pricing/productEntryMapping.ts:57` — `VENDOR_DEPARTMENT` (all → "Furniture")

The pricing-import UI is driven entirely off that array: a dealer carrying eight other mills opens Admin → Pricing → Import and sees nine brands they don't buy from and none they do, and `nameMatch` silently fails to bind when Vendor rows are spelled differently. The configurator's Options screen opens permanently empty with no vendor and no prompt (and the other two screens behave differently for the same input, so one deployment gets three inconsistent landing states). Product entry: no `VENDOR_DEPARTMENT` hit means department defaults to "Furniture" and category to "Living Room" for **100% of the catalog**, after which `CATEGORY_VALID_TYPES` coerces the inferred stock type to the first entry of a category the product isn't in — every suggestion wrong, and unusable outright if no department is literally named "Furniture". The dated `defaultPriceListName`s also write stale price-list names into a new deployment.

**Fix.** Bind on `Vendor.id`, never a name substring. Per-vendor import facts (which shipped parser, which endpoint, label) move onto the Vendor row or a `VendorImportFormat` table seeded by a preset; the view renders whatever the retailer's vendors declare. Drop `defaultPriceListName` and default the field to `'<Vendor> <Type> <current month year>'` computed at open time. For product entry: `Vendor.defaultDepartmentId` for department, the existing `Vendor.code`/alias field for POS supplier spelling, and a `vendor-taxonomy` preset of (vendor, collection) → category rows, resolved to real Department/Category ids at apply time. Keep `PRODUCT_CATEGORY_KEYWORDS` and `TYPE_KEYWORDS` in code — that is furniture vocabulary, not one tenant's roster. Configurator: delete the literal from all three screens; select the sole vendor when there is exactly one, otherwise leave unselected and render the existing empty-state prompt (a real default belongs on a `Vendor.isDefaultForPricing` flag or the user's last selection).

**Test.** Equivalence on suggestions: run the product-entry suggester over every existing `Product` and diff suggested (department, category, type) before vs after with Saybrook's mappings loaded as preset data — identical. UI: snapshot the import view's vendor list with Saybrook's vendors (unchanged), then with a fabricated eight-vendor tenant and assert exactly those eight render. Configurator: assert all three screens land in the same state for a zero-match tenant.

---

### 23. Apparel / home-accessory order formats carry vendor facts — NEW, data-correctness + money
`app/src/lib/apparelOrderVendors.ts:95` — `APPAREL_VENDOR_FORMATS` with `catalogVendorName: "Rails"`, `partNumberPrefix: "RAI"`, `newItemsUseColorCode`
`app/src/lib/apparelOrderVendors.ts:168` — `partNumberPrefix: "HBEL"` with a comment saying it was carried over verbatim because "holt has no Vendor.partNumberPrefix column"
`app/src/lib/homeAccessoryOrders.ts:85` — `HOME_ACCESSORY_FORMATS` with `catalogVendorName: "K & K Interiors"`, `"Wendover Art Group"`, …
`app/src/lib/homeAccessoryOrders.ts:297` — `SPLIT_PRESETS = { 2: [{ label: "62 / 38", percents: [62,38] }, …] }`
`app/src/lib/pricing/beatrizBallOrderParser.ts:64` — `"sold to", "ship to", "saybrook", "old saybrook"`

Part numbers: a brand absent from the registry falls to the unprefixed path, so **the same physical product yields two different part numbers depending on which document format it arrived in**; `extractSizeAndColor` (line 316) then fails to round-trip because `partNumber.startsWith(head)` is false, and a reorder of an existing style creates duplicate draft items instead of matching. `catalogVendorName` prefills a vendor name that doesn't exist in another tenant's Vendor table, producing draft POs attached to an invented or wrong vendor; outside these eight, the Home Accessory Order Import tool is dead. `SPLIT_PRESETS` is money: accepting the prefill books 62/38 of a two-piece set price onto `BuyerDraftItem.costPerUnit`, which flows into margin and buy-performance math. The Beatriz Ball parser enumerates the buyer's own town, so another tenant's ship-to lines are read as item rows — phantom line items, corrupted descriptions, and the printed-total reconciliation warning firing on every file.

**Fix.** Add the `Vendor.partNumberPrefix String?` column the header says is missing (or reuse `Vendor.code`, `schema.prisma:22`, which already holds short codes like 'WH'); `buildPrefixedPartNumber` takes it from the resolved Vendor row and the registry drops the field entirely. Resolve `catalogVendorName` from the selected Vendor and drop it from the shipped registry — require the buyer to select the vendor, or resolve it from the document as MarketTime already does; a default belongs on the Vendor row as `sourceFormatId`. `newItemsUseColorCode` becomes `Vendor.colorKeyedByCode Boolean`. Keep `normalizeStyleNumber` and genuine format quirks in code keyed by parser — that is logic. Split presets: make **Even** the shipped default prefill and move named shapes to a `splitPresets` JSON on the Vendor row. Beatriz Ball: drop the ship-to block structurally (skip everything between the 'ship to'/'sold to' marker and the 'item code' header row) — that is vendor-format logic true for every buyer; if a literal list is still wanted, seed it from the tenant's own `StoreLocation` address at call time.

**Test (equivalence).** Re-parse every stored apparel and home-accessory upload old vs new and diff: composed `partNumber` per line (identical), resolved `vendorId` per draft PO (identical), `costPerUnit` per drafted item (identical — the split default change must apply only to *new* prefills, so assert historical drafts are untouched and add a separate test that a fresh 2-piece draft now prefills 50/50). For Beatriz Ball: diff extracted line-item count and descriptions per file, and assert the printed-total reconciliation warning fires on exactly the same files as before.

---

### 24. Traffic counter endpoint and hours — NEW, feature-dead + data-correctness
`app/src/lib/axperClient.ts:43` — `const AXPER_URL = "https://cloud.axper.com/api/TrafficReport/GetTrafficDataUsingDailyPeriod";`
`app/src/lib/axperClient.ts:117` — `HourMinuteFrom: opts.hourMinuteFrom ?? "09:00", HourMinuteTo: opts.hourMinuteTo ?? "18:00"`

An operator who fills in "API Base URL" sees it **silently ignored**; a regional/on-prem instance or a different counter cannot be pointed anywhere, and the Integration Test page reports pass/fail against someone else's cloud. The hours default truncates the denominator: a store open 10–9 loses every visitor after 6pm, so conversion rate (sales / traffic) reads systematically **too high** on the dashboard and in the traffic report.

**Fix.** Resolve the base URL through the same credential path as `apiKey` — `resolveCredential('axper','apiBase')` — defaulting to the current literal when unset, keeping only the path segment in code. Take the window from `StoreLocation` opening hours (or an `AppSettings.trafficWindow`) and pass it at each call site; cheapest correct version is to widen the shipped default to `00:00`–`23:59` and let the store record narrow it, since the counter data is per-15-min anyway.

**Test (equivalence).** With no `apiBase` credential set, assert the composed request URL is byte-identical to today's. Then set a scratch base URL and assert the request goes there. For hours: re-pull the last 90 days with the widened window and compare daily visitor counts — **diffs are expected and are the point**; publish the per-day delta and the recomputed conversion rate in the PR body, and confirm with the owner before the traffic report's history changes. If history must be preserved, gate the widening behind the store record so existing rows are untouched.

**Also here (KNOWN).** `schema.prisma:4697` `axperStoreName` (NOT NULL, in the upsert key) → rename to `trafficSourceName` and repoint the unique key. The generalized half (`StoreLocation.trafficSourceNames`, preset-populated) is already in place, so this is rename-only — but it is a migration, so it rides in Wave 3.

---

### 25. Google project folder ids and subfolder list — NEW, feature-dead + cosmetic
`app/src/pages/api/google/create-project.ts:10` — `ROOT_FOLDER_ID = "1zlCFD…"`, `TEMPLATE_PRESENTATION_ID = "14R696…"`
`app/src/pages/api/google/create-project.ts:58` — `subfolders = ["Windows","Rugs","Fabrics","Furniture","Photos","Presentation"]`

`drive.files.create` (line 41) passes `parents: [ROOT_FOLDER_ID]`; another deployment's OAuth token has no access, so it 404s/403s on first use, and `drive.files.copy` of the template (line 79) fails the same way. Create Project Folder is unusable out of the box and unfixable without a redeploy. `"Presentation"` is additionally load-bearing — renaming it breaks the template copy with "Could not find or create the Presentation subfolder."

**Fix.** Two nullable fields on the existing Google `IntegrationCredential` (or AppSettings): `googleProjectRootFolderId`, `googlePresentationTemplateId`, set in Admin. Return **400 with "Google project folders are not configured"** when either is absent, instead of throwing a Drive error. Add a `projectFolderSubfolders` string array (defaulted in code to the current list) plus a separate `presentationSubfolder` field naming which one receives the template copy, so the load-bearing folder is selected by config rather than matched against a magic literal.

**Test.** With the current ids configured, create a project folder on the real Saybrook Drive and assert the resulting tree (six subfolders, template copied into the named one) is identical to today. Unconfigured: assert 400 with the config message and no Drive call made.

---

### 26. Inventory snapshot bound to `Stockid`/`Qty`/`Location` header names — NEW, feature-dead
`app/src/lib/inventory/snapshotImport.ts:46` — `const stockIdStr = record.Stockid || record.stockid;`

A tenant whose stock export uses SKU/Qty/Bin gets `undefined` on every row, so `resolveSnapshotImportRow` returns `{ status: "skip" }` for the whole file. The import **completes reporting zero rows and no error**, and the inventory variance workflow has nothing to compare against.

**Fix.** Resolve the three columns through the existing `ImportDefinition` field mapping *before* the row reaches this module: the caller hands `resolveSnapshotImportRow` a normalized `{ externalId, quantity, location }`. The header→field binding lives in the import definition the preset applies; the resolve/skip/invalid classification stays in code untouched.

**Test (equivalence).** Re-import the last several production snapshot files old vs new and diff per-file counts by status (resolved / skip / invalid) and the resulting `InventorySnapshot` rows — identical. Then a scratch file with SKU/Qty/Bin headers plus a matching import definition must resolve 100% of rows.

---

## Wave 3 — migrations with backfill (slow, serialized; feature-dead / cosmetic)

Each of these needs its own migration + backfill and a mechanical rename across call sites. Nothing in Waves 1–2 depends on them. Do them one per PR, in this order.

### 27. `GoodsInward` idempotency key is vendor-named — NEW, data-correctness (money-adjacent)
`app/prisma/schema.prisma:1764` — `@@unique([externalGipNo, externalPorNo])`

The model's only idempotency key is a pair of **nullable** vendor-named columns. Any other source adapter (`AppSettings.sourceAdapterId` is designed to be pluggable; `lib/adapters/` already has `noneAdapter.ts` alongside ordorite) has **no re-import safety at all** — NULLs compare distinct in a Postgres unique index, so a re-run appends duplicate receiving rows, which `lib/reports/buyersReport.ts:293-296`'s `received` CTE sums straight into allocated stock.

**Fix.** Use the provenance shape this schema already uses twice — `ServiceCase.externalSource`/`externalSourceId` (`schema.prisma:3378-3380`) and `InventorySnapshot.source`/`externalId` (`1324-1330`): add `externalSource String?` + `externalRef String?` with `@@unique([externalSource, externalRef])`, and have the Ordorite runner write `source = "ordorite"` and its composed key there. The adapter id (already config) selects the namespace; no adapter-specific column stays on the core model.

**Migration risk.** Backfill must compose `externalRef` from the existing pair and **prove uniqueness before adding the constraint** — if production already holds duplicate/NULL pairs the migration fails mid-deploy. Run the uniqueness check as a read-only query first and put the result in the PR body. Keep the old columns nullable and unused for one release before dropping.

**Test (equivalence).** After backfill, diff `buyersReport`'s `received` quantities per PO line before vs after — identical. Then re-run a completed Ordorite inbound import twice and assert zero new `GoodsInward` rows on the second run (today, with NULLs, it appends).

### 28. Vendor-branded models, relations and enums — NEW (5) + KNOWN (1)
All rename-only; the columns themselves are reasonable retail signals.

| site | rename |
|---|---|
| `app/prisma/schema.prisma:1072` (`model WindfallEnrichment`, `windfallId String? @unique`) and `:1027` (`windfallEnrichment WindfallEnrichment?`) | → `CustomerEnrichment` / `customerEnrichment`, with `provider String` + `providerRecordId String` replacing `windfallId` and `@@unique([provider, providerRecordId])`. Mechanical rename across `lib/opportunityTiles.ts:144,156,166,177,210` and `lib/reports/wealthInsights.ts:45` (`SIGNAL_FIELDS`). Until this lands, five opportunity segments — Big wallets, Second home, Landlord special, Boat crowd, Life event — count zero and Wealth Insights renders blank for anyone not pretending to be Windfall. The Windfall CSV parser becomes one adapter writing `provider = "windfall"`. |
| `app/prisma/schema.prisma:1131` (`model MailchimpActivity`) and `:4123` (`LeadSource.MAILCHIMP_CLICK`, + the open value) | → collapse the two enum members to one mechanism value (`EMAIL_ENGAGEMENT`, or `EMAIL_CLICK`/`EMAIL_OPEN`) and carry the provider in the existing free-text `Lead.sourceDetail`, mirroring `ServiceCase.externalSource` (`3378`) which already does exactly this job as a String. Today a Klaviyo/Constant Contact/Braze tenant must file engagement leads as OTHER, collapsing them with walk-ins in every lead report; adding an ESP is a Postgres enum migration on a core model, which is the inversion rule 62 forbids. `lib/mailchimpLeadIngestor.ts` keeps its own vendor-specific parsing. |
| `app/prisma/schema.prisma:1815` (`HD_PROPOSAL`) | → `VENDOR_PROPOSAL_IMPORT`, carrying the actual vendor on the existing draft→Vendor relation. **Ship in the same PR as #11's `import-hd-proposal.ts` change**, not separately. |
| `app/prisma/schema.prisma:3923` (`rugNumber`), `:3973` (`fmRecordId`) | → `vendorItemRef` / `lotRef` with a config-supplied display label, and `externalSource` + `externalSourceId` replacing `fmRecordId`. Replace the literal workbook-header matching with `ImportDefinition.fieldMappings` (`schema.prisma:5448`, `5484`) so column names become preset data. Move the 7× / 7-over-2 cost multipliers to a per-vendor consignment pricing setting — config selects the multiplier, the pure calculation stays in code. Today a tenant consigning art or lighting must abuse `rugNumber`, and every row lacking `rugNumber`/`quality` is dropped. |
| `app/prisma/schema.prisma:3021` (`model SEComponent`, + `notAvailableInLeather`/`notAvailableOnSleepers` at `3032-3033`) | The schema shape is already right — delete `SE_COMPONENT_SEEDS` from the import route and seed `SEComponent` rows from a vendor-scoped preset (`lib/config/presetFiles.ts`), with `componentType` a lookup rather than a comment-documented literal set. Register the PDF parser through the existing `ImportDefinition.runnerKey` escape hatch (`schema.prisma:5480`) so a second vendor's build-your-own program is a preset + registered runner, not a code edit. The two vendor rule columns become rows in a generic component-restriction table. Ship after #21's `VendorStyle.programId`. |
| `app/prisma/schema.prisma:2464` `skipSameDayRewriteCleanup` — **KNOWN** | Move off `SalesOrder` into adapter-owned state keyed by order id, so the highest-traffic core table carries no import-runner concept. |
| `app/prisma/schema.prisma:4697` `axperStoreName` — **KNOWN** | Rename to `trafficSourceName`, repoint the unique key; see #24. |

**Test for every rename in #28.** These are refactors, so the bar is *byte-identical output*: for each, snapshot the affected report/tile values before the migration, run migration + backfill, and diff. Specifically — all ten opportunity-tile counts and every Wealth Insights signal count (Windfall); lead counts by source for 24 months (Mailchimp); consignment item counts, `vendorItemRef` values and computed payables per vendor (rugNumber/fmRecordId); the full SE component list and every priced SE configuration (SEComponent). Plus, for each: a scratch-DB run with a second provider/vendor registered, proving the feature is now reachable without a migration. Confirm the backfill is re-runnable (rule 63) before merging.

### Report taxonomies — DONE (migration `20260821120000_department_report_roles`)
`lib/reports/crossSell.ts` and `lib/reports/designerDashboard.ts` no longer carry department names. Three columns on `Department` hold the facts: `reportGroup` (which dashboard column a department rolls up into; `NULL` excludes it), `crossSellTarget`, and `crossSellAnchor` (the department whose spend qualifies a customer — it was `'Furniture'`, hardcoded three times in raw SQL). `lib/reports/reportTaxonomy.ts` loads them once per request.

The backfill reproduces the old classifiers exactly, so existing numbers are unchanged, and `__tests__/integration/departmentReportRoles.integration.test.ts` proves it by running the migration's own UPDATE statements — read from the file, not retyped — against a real database and diffing against the old logic.

Worth recording why this mattered more than it looked: **none of the hardcoded names exist in the demo seed.** On a fresh clone the designer dashboard swept almost every department into one fallback column, and the cross-sell report anchored on a `Furniture` department that is not there and returned zero rows every time, silently. Two behaviour changes are deliberate: a department with no `reportGroup` is excluded rather than falling through to a default bucket, and both reports now say when nothing is configured instead of rendering a confident zero.

### Also on the backlog, unchanged (KNOWN)
### Vendor number prefixes — DONE (migration `20260822140000_vendor_number_prefix`)

`lib/consignment.ts` hardcoded one consignment vendor's numbering: `MAR-` on POS product numbers, `M` on the physical tag, across 26 call sites in `paymentService`, the Ordorite runners and three consignment import routes. A second vendor matched nothing, **silently** — no error, just items that never linked.

**The note that used to be here was wrong**, and worth recording as a lesson: it said this was "retired by #10's `Vendor.isConsignment`". That column does not exist and the PR never landed, so the entry sat behind a false "already handled" claim.

`VendorNumberPrefix` holds it now — `prefix`, optional `barcodePrefix`, unique on `prefix` so two vendors cannot both claim `MAR-`. Prefixes exist to make numbers unique across vendors (two suppliers both ship a part "1827"), which is why this is a table rather than a string convention.

**Opt-in:** a deployment with no rows has the feature off and every lookup returns "not a vendor number". The pure rules live in `lib/vendorNumbering.ts`; `lib/vendorPrefixService.ts` loads and caches them.
